repeat task.wait() until game:IsLoaded()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Player = Players.LocalPlayer
local function waitForCharacter()
    local char = Player.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then return char end
    return Player.CharacterAdded:Wait()
end
task.spawn(waitForCharacter)
if not getgenv then getgenv = function() return _G end end
-- ============================================================
-- STATE
-- ============================================================
local ConfigFileName = "INTERSTELLAR_Config.json"
local Enabled = {
    SpeedBoost = false,
    AntiRagdoll = false,
    SpinBot = false,
    SpeedWhileStealing = false,
    AutoSteal = false,
    Unwalk = false,
    Optimizer = false,
    Galaxy = false,
    SpamBat = false,
    AutoDisableSpeed = true,
    AutoWalkEnabled = false,
    AutoRightEnabled = false,
    ScriptUserESP = true,
    InfiniteJump = false,
    NoDie = false,
    Aimbot = false,
    Float = false,
    FullAutoDuel = false,
    MapFOV = false,
}
local Values = {
    BoostSpeed = 30,
    SpinSpeed = 30,
    StealingSpeedValue = 29,
    STEAL_RADIUS = 20,
    STEAL_DURATION = 1.3,
    DEFAULT_GRAVITY = 196.2,
    GalaxyGravityPercent = 70,
    HOP_POWER = 35,
    HOP_COOLDOWN = 0.08,
    FOV = 70,
   
}
local KEYBINDS = {
    SPEED = Enum.KeyCode.V,
    SPIN = Enum.KeyCode.N,
    GALAXY = Enum.KeyCode.M,
    ANTIRAGDOLL = Enum.KeyCode.Unknown,
    AIMBOT = Enum.KeyCode.Unknown,
    FLOAT = Enum.KeyCode.Unknown,
    FULLAUTODUEL = Enum.KeyCode.Unknown,
}
local armedKeybinds = {}
-- ============================================================
-- CONFIG
-- ============================================================
local function SaveConfig()
    local data = {}
    for k,v in pairs(Enabled) do data[k] = v end
    for k,v in pairs(Values) do data[k] = v end
    for k,v in pairs(KEYBINDS) do data["KEY_"..k] = v.Name end
    local ok = false
    if writefile then
        pcall(function() writefile(ConfigFileName, HttpService:JSONEncode(data)); ok=true end)
    end
    return ok
end
local function LoadConfig()
    pcall(function()
        if readfile and isfile and isfile(ConfigFileName) then
            local data = HttpService:JSONDecode(readfile(ConfigFileName))
            if data then
                for k,v in pairs(data) do
                    if Enabled[k] ~= nil then Enabled[k] = v end
                    if Values[k] ~= nil then Values[k] = v end
                end
                for k in pairs(KEYBINDS) do
                    local key = "KEY_"..k
                    if data[key] then
                        local ok, kc = pcall(function() return Enum.KeyCode[data[key]] end)
                        if ok and kc then KEYBINDS[k] = kc end
                    end
                end
            end
        end
    end)
end
LoadConfig()
-- ============================================================
-- AXIOM HUB STYLE GUI (ALTO CENTRALE) - VERSIONE FUNZIONANTE
-- ============================================================
local axiomGui = Instance.new("ScreenGui")
axiomGui.Name = "AxiomTopBar"
axiomGui.ResetOnSpawn = false
axiomGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
axiomGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(0, 340, 0, 36)
topBar.Position = UDim2.new(0.5, -172, 0, 115)
topBar.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
topBar.BackgroundTransparency = 0.08
topBar.BorderSizePixel = 0
topBar.Parent = axiomGui
local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(0, 10)
barCorner.Parent = topBar
local barStroke = Instance.new("UIStroke")
barStroke.Thickness = 1
barStroke.Color = Color3.fromRGB(55, 55, 68)
barStroke.Transparency = 0.4
barStroke.Parent = topBar
-- Stanghetta bianca piccola affianco al titolo
local topBarAccent = Instance.new("Frame")
topBarAccent.Size = UDim2.new(0, 3, 0, 14)
topBarAccent.Position = UDim2.new(0, 10, 0.5, -7)
topBarAccent.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
topBarAccent.BorderSizePixel = 0
topBarAccent.Parent = topBar
local topBarAccentCorner = Instance.new("UICorner", topBarAccent)
topBarAccentCorner.CornerRadius = UDim.new(0, 4)
-- VORTEX HUB text
local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(0, 120, 1, 0)
titleText.Position = UDim2.new(0, 18, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "VortexHub"
titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
titleText.Font = Enum.Font.GothamBlack
titleText.TextSize = isMobile and 20 or 14 -- più grande su mobile
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.TextYAlignment = Enum.TextYAlignment.Center
titleText.Parent = topBar
-- Separator 1
local sep1 = Instance.new("Frame")
sep1.Size = UDim2.new(0, 1, 0, 20)
sep1.Position = UDim2.new(0, 146, 0.5, -10)
sep1.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
sep1.BackgroundTransparency = 0.6
sep1.BorderSizePixel = 0
sep1.Parent = topBar
-- FPS Label
local fpsText = Instance.new("TextLabel")
fpsText.Size = UDim2.new(0, 80, 1, 0)
fpsText.Position = UDim2.new(0, 141, 0, 0)
fpsText.BackgroundTransparency = 1
fpsText.Text = "FPS 60"
fpsText.TextColor3 = Color3.fromRGB(100, 255, 100)
fpsText.Font = Enum.Font.GothamBlack
fpsText.TextSize = 13
fpsText.TextXAlignment = Enum.TextXAlignment.Center
fpsText.TextYAlignment = Enum.TextYAlignment.Center
fpsText.Parent = topBar
-- Separator 2
local sep2 = Instance.new("Frame")
sep2.Size = UDim2.new(0, 1, 0, 20)
sep2.Position = UDim2.new(0, 220, 0.5, -10)
sep2.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
sep2.BackgroundTransparency = 0.6
sep2.BorderSizePixel = 0
sep2.Parent = topBar
-- Ping Label
local pingText = Instance.new("TextLabel")
pingText.Size = UDim2.new(0, 90, 1, 0)
pingText.Position = UDim2.new(0, 230, 0, 0)
pingText.BackgroundTransparency = 1
pingText.Text = "PING 23ms"
pingText.TextColor3 = Color3.fromRGB(100, 255, 100)
pingText.Font = Enum.Font.GothamBlack
pingText.TextSize = 13
pingText.TextXAlignment = Enum.TextXAlignment.Center
pingText.TextYAlignment = Enum.TextYAlignment.Center
pingText.Parent = topBar
-- Aggiornamento FPS e PING
local frameCount = 0
local lastTime = tick()
local smoothFPS = 60
local smoothPing = 0
game:GetService("RunService").RenderStepped:Connect(function(dt)
    frameCount = frameCount + 1
    local now = tick()
   
    if now - lastTime >= 0.5 then
        local elapsed = now - lastTime
        local rawFPS = frameCount / elapsed
        smoothFPS = math.floor(smoothFPS * 0.7 + rawFPS * 0.3 + 0.5)
        frameCount = 0
        lastTime = now
        fpsText.Text = "FPS " .. smoothFPS
        if smoothFPS >= 50 then
            fpsText.TextColor3 = Color3.fromRGB(100, 255, 100)
        elseif smoothFPS >= 30 then
            fpsText.TextColor3 = Color3.fromRGB(255, 200, 100)
        else
            fpsText.TextColor3 = Color3.fromRGB(255, 80, 80)
        end
    end
end)
task.spawn(function()
    while true do
        task.wait(1)
        pcall(function()
            local rawPing = Players.LocalPlayer:GetNetworkPing()
            if rawPing and rawPing > 0 then
                local pingVal = math.floor(rawPing * 1000)
                smoothPing = math.floor(smoothPing * 0.5 + pingVal * 0.5)
                pingText.Text = "PING " .. smoothPing .. "ms"
                if smoothPing <= 50 then
                    pingText.TextColor3 = Color3.fromRGB(100, 255, 100)
                elseif smoothPing <= 100 then
                    pingText.TextColor3 = Color3.fromRGB(255, 200, 100)
                else
                    pingText.TextColor3 = Color3.fromRGB(255, 80, 80)
                end
            end
        end)
    end
end)
-- Effetto hover
topBar.MouseEnter:Connect(function()
    TweenService:Create(topBar, TweenInfo.new(0.2), {BackgroundTransparency = 0.05}):Play()
end)
topBar.MouseLeave:Connect(function()
    TweenService:Create(topBar, TweenInfo.new(0.2), {BackgroundTransparency = 0.2}):Play()
end)
-- ============================================================
-- BACKEND (Tutte le funzionalità originali rimangono invariate)
-- ============================================================
local Connections = { infiniteJump = {heartbeat=nil,jumpRequest=nil} }
local lastBatSwing = 0
local BAT_SWING_COOLDOWN = 0.12
local SlapList = {
    {1,"Bat"},{2,"Slap"},{3,"Iron Slap"},{4,"Gold Slap"},
    {5,"Diamond Slap"},{6,"Emerald Slap"},{7,"Ruby Slap"},
    {8,"Dark Matter Slap"},{9,"Flame Slap"},{10,"Nuclear Slap"},
    {11,"Galaxy Slap"},{12,"Glitched Slap"},
}
local function getNearestPlayer()
    local c = Player.Character if not c then return nil end
    local h = c:FindFirstChild("HumanoidRootPart") if not h then return nil end
    local nearest, dist = nil, math.huge
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= Player and p.Character then
            local oh = p.Character:FindFirstChild("HumanoidRootPart")
            if oh then local d=(h.Position-oh.Position).Magnitude if d<dist then dist=d; nearest=p end end
        end
    end
    return nearest
end
local function findBat()
    local c = Player.Character if not c then return nil end
    local bp = Player:FindFirstChildOfClass("Backpack")
    for _,ch in ipairs(c:GetChildren()) do
        if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
    end
    if bp then
        for _,ch in ipairs(bp:GetChildren()) do
            if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
        end
    end
    for _,i in ipairs(SlapList) do
        local t = c:FindFirstChild(i[2]) or (bp and bp:FindFirstChild(i[2]))
        if t then return t end
    end
    return nil
end
local function stopSpamBat()
    if Connections.spamBat then Connections.spamBat:Disconnect(); Connections.spamBat=nil end
end
local spamBatCircle, spamBatCircleConn
local function createSpamBatCircle()
    if spamBatCircle then spamBatCircle:Destroy(); spamBatCircle=nil end
    local c = Player.Character if not c then return end
    local hrp = c:FindFirstChild("HumanoidRootPart") if not hrp then return end
    local circle = Instance.new("Part")
    circle.Name="ZAY_SpamBatCircle"; circle.Anchored=true; circle.CanCollide=false
    circle.CastShadow=false; circle.Material=Enum.Material.Neon
    circle.Color=Color3.fromRGB(200,150,255)
    circle.Shape=Enum.PartType.Cylinder
    circle.Size=Vector3.new(0.08,20,20); circle.Transparency=0.18; circle.Parent=workspace
    spamBatCircle = circle
end
local function removeSpamBatCircle()
    if spamBatCircle then spamBatCircle:Destroy(); spamBatCircle=nil end
    if spamBatCircleConn then spamBatCircleConn:Disconnect(); spamBatCircleConn=nil end
end
local function startSpamBatCircle()
    removeSpamBatCircle(); createSpamBatCircle()
    spamBatCircleConn = RunService.Heartbeat:Connect(function()
        if not Enabled.SpamBat then removeSpamBatCircle(); return end
        local c = Player.Character if not c then return end
        local hrp = c:FindFirstChild("HumanoidRootPart") if not hrp then return end
        if not spamBatCircle or not spamBatCircle.Parent then createSpamBatCircle(); return end
        spamBatCircle.CFrame = CFrame.new(hrp.Position.X,hrp.Position.Y-3.2,hrp.Position.Z)*CFrame.Angles(0,0,math.rad(90))
    end)
end
local spinBAV
local function startSpinBot()
    local c = Player.Character if not c then return end
    local hrp = c:FindFirstChild("HumanoidRootPart") if not hrp then return end
    if spinBAV then spinBAV:Destroy(); spinBAV=nil end
    for _,v in pairs(hrp:GetChildren()) do if v.Name=="SpinBAV" then v:Destroy() end end
    spinBAV = Instance.new("BodyAngularVelocity")
    spinBAV.Name="SpinBAV"; spinBAV.MaxTorque=Vector3.new(0,math.huge,0)
    spinBAV.AngularVelocity=Vector3.new(0,Values.SpinSpeed,0); spinBAV.Parent=hrp
end
local function stopSpinBot()
    if spinBAV then spinBAV:Destroy(); spinBAV=nil end
    local c = Player.Character
    if c then local hrp=c:FindFirstChild("HumanoidRootPart") if hrp then for _,v in pairs(hrp:GetChildren()) do if v.Name=="SpinBAV" then v:Destroy() end end end end
end
local AutoWalkEnabled = false
local AutoRightEnabled = false
local onAutoRightDone = nil
local onAutoLeftDone = nil
RunService.Heartbeat:Connect(function()
    if Enabled.SpinBot and spinBAV then
        spinBAV.AngularVelocity = Player:GetAttribute("Stealing") and Vector3.new(0,0,0) or Vector3.new(0,Values.SpinSpeed,0)
    end
end)
local galaxyVectorForce, galaxyAttachment
local galaxyEnabled = false
local hopsEnabled = false
local lastHopTime = 0
local spaceHeld = false
local originalJumpPower = 50
local function captureJumpPower()
    local c = Player.Character if not c then return end
    local hum = c:FindFirstChildOfClass("Humanoid")
    if hum and hum.JumpPower > 0 then originalJumpPower = hum.JumpPower end
end
task.spawn(function() task.wait(1); captureJumpPower() end)
Player.CharacterAdded:Connect(function() task.wait(1); captureJumpPower() end)
local function setupGalaxyForce()
    pcall(function()
        local c = Player.Character if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart") if not h then return end
        if galaxyVectorForce then galaxyVectorForce:Destroy() end
        if galaxyAttachment then galaxyAttachment:Destroy() end
        galaxyAttachment = Instance.new("Attachment"); galaxyAttachment.Parent = h
        galaxyVectorForce = Instance.new("VectorForce")
        galaxyVectorForce.Attachment0 = galaxyAttachment
        galaxyVectorForce.ApplyAtCenterOfMass = true
        galaxyVectorForce.RelativeTo = Enum.ActuatorRelativeTo.World
        galaxyVectorForce.Force = Vector3.new(0,0,0); galaxyVectorForce.Parent = h
    end)
end
local function updateGalaxyForce()
    if not galaxyEnabled or not galaxyVectorForce then return end
    local c = Player.Character if not c then return end
    local mass = 0
    for _,p in ipairs(c:GetDescendants()) do if p:IsA("BasePart") then mass+=p:GetMass() end end
    local tg = Values.DEFAULT_GRAVITY*(Values.GalaxyGravityPercent/100)
    galaxyVectorForce.Force = Vector3.new(0,mass*(Values.DEFAULT_GRAVITY-tg)*0.95,0)
end
local function adjustGalaxyJump()
    pcall(function()
        local c = Player.Character if not c then return end
        local hum = c:FindFirstChildOfClass("Humanoid") if not hum then return end
        if not galaxyEnabled then hum.JumpPower=originalJumpPower; return end
        local ratio = math.sqrt((Values.DEFAULT_GRAVITY*(Values.GalaxyGravityPercent/100))/Values.DEFAULT_GRAVITY)
        hum.JumpPower = originalJumpPower*ratio
    end)
end
local function doMiniHop()
    if not hopsEnabled then return end
    pcall(function()
        local c = Player.Character if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart")
        local hum = c:FindFirstChildOfClass("Humanoid")
        if not h or not hum then return end
        if tick()-lastHopTime < Values.HOP_COOLDOWN then return end
        lastHopTime = tick()
        if hum.FloorMaterial == Enum.Material.Air then
            h.AssemblyLinearVelocity = Vector3.new(h.AssemblyLinearVelocity.X,Values.HOP_POWER,h.AssemblyLinearVelocity.Z)
        end
    end)
end
local function startGalaxy() galaxyEnabled=true; hopsEnabled=true; setupGalaxyForce(); adjustGalaxyJump() end
local function stopGalaxy()
    galaxyEnabled=false; hopsEnabled=false
    if galaxyVectorForce then galaxyVectorForce:Destroy(); galaxyVectorForce=nil end
    if galaxyAttachment then galaxyAttachment:Destroy(); galaxyAttachment=nil end
    adjustGalaxyJump()
end
RunService.Heartbeat:Connect(function()
    if hopsEnabled and spaceHeld then doMiniHop() end
    if galaxyEnabled then updateGalaxyForce() end
end)
local function getMovementDirection()
    local c = Player.Character if not c then return Vector3.zero end
    local hum = c:FindFirstChildOfClass("Humanoid")
    return hum and hum.MoveDirection or Vector3.zero
end
local function startSpeedBoost()
    if Connections.speed then return end
    Connections.speed = RunService.Heartbeat:Connect(function()
        if not Enabled.SpeedBoost then return end
        if Player:GetAttribute("Stealing") then return end
        pcall(function()
            local c = Player.Character if not c then return end
            local h = c:FindFirstChild("HumanoidRootPart") if not h then return end
            local md = getMovementDirection()
            if md.Magnitude > 0.1 then
                h.AssemblyLinearVelocity = Vector3.new(md.X*Values.BoostSpeed,h.AssemblyLinearVelocity.Y,md.Z*Values.BoostSpeed)
            end
        end)
    end)
end
local function stopSpeedBoost()
    if Connections.speed then Connections.speed:Disconnect(); Connections.speed=nil end
end
-- ============================================================
-- ANIMATION SPEED FIX (solo visuale, non tocca WalkSpeed)
-- ============================================================
RunService.Heartbeat:Connect(function()
    pcall(function()
        local char = Player.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not hum then return end
        for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
            track:AdjustSpeed(0.8) -- 0.5 = metà velocità animazione, cambia a piacere
        end
    end)
end)
-- ============================================================
-- FLOAT BACKEND (INTEGRAZIONE CON IL TOGGLE)
-- ============================================================
local hitboxExpanding = false
local hitboxConn = nil
local originalSizes = {}
local hitboxESPAdornments = {}

local function createHitboxESP(hrp)
    if hitboxESPAdornments[hrp] then return end
    local sphere = Instance.new("SphereHandleAdornment")
    sphere.Adornee = hrp
    sphere.Radius = 5
    sphere.Color3 = Color3.fromRGB(0, 220, 255)
    sphere.Transparency = 0.55
    sphere.AlwaysOnTop = true
    sphere.ZIndex = 5
    sphere.Parent = hrp
    hitboxESPAdornments[hrp] = sphere
end

local function removeHitboxESP(hrp)
    if hitboxESPAdornments[hrp] then
        hitboxESPAdornments[hrp]:Destroy()
        hitboxESPAdornments[hrp] = nil
    end
end

local function startHitboxExpander()
    if hitboxExpanding then return end
    hitboxExpanding = true
    hitboxConn = RunService.Heartbeat:Connect(function()
        if not hitboxExpanding then return end
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= Player and p.Character then
                local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                if hrp and not originalSizes[hrp] then
                    -- Salva size originale solo dell'HRP
                    originalSizes[hrp] = {size = hrp.Size, trans = hrp.Transparency}
                    -- Espandi solo HRP e rendilo invisibile
                    hrp.Size = Vector3.new(10, 10, 10)
                    hrp.Transparency = 1
                    -- ESP sfera ciano
                    createHitboxESP(hrp)
                end
            end
        end
        -- Pulisci ESP di player usciti
        for hrp, _ in pairs(hitboxESPAdornments) do
            if not hrp or not hrp.Parent then
                removeHitboxESP(hrp)
            end
        end
    end)
end
local function stopHitboxExpander()
    hitboxExpanding = false
    if hitboxConn then hitboxConn:Disconnect(); hitboxConn = nil end
    for part, data in pairs(originalSizes) do
        pcall(function()
            if part and part.Parent then
                part.Size = data.size
                part.Transparency = data.trans
            end
        end)
    end
    originalSizes = {}
    for hrp, _ in pairs(hitboxESPAdornments) do
        pcall(function() removeHitboxESP(hrp) end)
    end
    hitboxESPAdornments = {}
end
-- ===========================================================
local POSITION_1 = Vector3.new(-476.48,-6.28, 92.73)
local POSITION_2 = Vector3.new(-483.12,-4.95, 94.80)
local POSITION_R1 = Vector3.new(-476.16,-6.52, 25.62)
local POSITION_R2 = Vector3.new(-483.04,-5.09, 23.14)
local autoWalkPhase = 1
local autoRightPhase = 1
local autoWalkConnection, autoRightConnection
local coordESPFolder = Instance.new("Folder",workspace); coordESPFolder.Name="INTERSTELLAR_CoordESP"
local function createCoordMarker(pos,lbl,col)
    local dot = Instance.new("Part",coordESPFolder)
    dot.Anchored=true; dot.CanCollide=false; dot.CastShadow=false
    dot.Material=Enum.Material.Neon; dot.Color=col; dot.Shape=Enum.PartType.Ball
    dot.Size=Vector3.new(1,1,1); dot.Position=pos; dot.Transparency=0.2
    local bb=Instance.new("BillboardGui",dot); bb.AlwaysOnTop=true; bb.Size=UDim2.new(0,100,0,20)
    bb.StudsOffset=Vector3.new(0,2,0); bb.MaxDistance=300
    local tx=Instance.new("TextLabel",bb); tx.Size=UDim2.new(1,0,1,0)
    tx.BackgroundTransparency=1; tx.Text=lbl; tx.TextColor3=col
    tx.TextStrokeColor3=Color3.fromRGB(0,0,0); tx.TextStrokeTransparency=0
    tx.Font=Enum.Font.GothamBold; tx.TextSize=12
end
createCoordMarker(POSITION_1, "L1", Color3.fromRGB(200,150,255))
createCoordMarker(POSITION_2, "L END", Color3.fromRGB(160,120,200))
createCoordMarker(POSITION_R1, "R1", Color3.fromRGB(220,170,255))
createCoordMarker(POSITION_R2, "R END", Color3.fromRGB(180,130,220))
local function faceSouth()
    local c=Player.Character if not c then return end
    local h=c:FindFirstChild("HumanoidRootPart") if not h then return end
    h.CFrame=CFrame.new(h.Position)*CFrame.Angles(0,0,0)
    local cam=workspace.CurrentCamera
    if cam then cam.CFrame=CFrame.new(h.Position.X,h.Position.Y+5,h.Position.Z-12)*CFrame.Angles(math.rad(-15),0,0) end
end
local function faceNorth()
    local c=Player.Character if not c then return end
    local h=c:FindFirstChild("HumanoidRootPart") if not h then return end
    h.CFrame=CFrame.new(h.Position)*CFrame.Angles(0,math.rad(180),0)
    local cam=workspace.CurrentCamera
    if cam then cam.CFrame=CFrame.new(h.Position.X,h.Position.Y+2,h.Position.Z+12)*CFrame.Angles(0,math.rad(180),0) end
end
local function startAutoWalk()
    if autoWalkConnection then autoWalkConnection:Disconnect() end
    autoWalkPhase=1
    autoWalkConnection = RunService.Heartbeat:Connect(function()
        if not AutoWalkEnabled then return end
        local c=Player.Character if not c then return end
        local h=c:FindFirstChild("HumanoidRootPart"); local hum=c:FindFirstChildOfClass("Humanoid")
        if not h or not hum then return end
        local target = autoWalkPhase==1 and POSITION_1 or POSITION_2
        local dist = (Vector3.new(target.X,h.Position.Y,target.Z)-h.Position).Magnitude
        if dist < 1 then
            if autoWalkPhase==1 then autoWalkPhase=2; return end
            hum:Move(Vector3.zero,false); h.AssemblyLinearVelocity=Vector3.new(0,0,0)
            AutoWalkEnabled=false; Enabled.AutoWalkEnabled=false
            if VisualSetters and VisualSetters.AutoWalkEnabled then VisualSetters.AutoWalkEnabled(false,true) end
            if autoWalkConnection then autoWalkConnection:Disconnect(); autoWalkConnection=nil end
            if onAutoLeftDone then onAutoLeftDone() end
            faceSouth(); return
        end
        local dir=(target-h.Position); local md=Vector3.new(dir.X,0,dir.Z).Unit
        hum:Move(md,false); h.AssemblyLinearVelocity=Vector3.new(md.X*Values.BoostSpeed,h.AssemblyLinearVelocity.Y,md.Z*Values.BoostSpeed)
    end)
end
local function stopAutoWalk()
    if autoWalkConnection then autoWalkConnection:Disconnect(); autoWalkConnection=nil end
    autoWalkPhase=1
    local c=Player.Character if c then local hum=c:FindFirstChildOfClass("Humanoid") if hum then hum:Move(Vector3.zero,false) end end
end
local function startAutoRight()
    if autoRightConnection then autoRightConnection:Disconnect() end
    autoRightPhase=1
    autoRightConnection = RunService.Heartbeat:Connect(function()
        if not AutoRightEnabled then return end
        local c=Player.Character if not c then return end
        local h=c:FindFirstChild("HumanoidRootPart"); local hum=c:FindFirstChildOfClass("Humanoid")
        if not h or not hum then return end
        local target = autoRightPhase==1 and POSITION_R1 or POSITION_R2
        local dist = (Vector3.new(target.X,h.Position.Y,target.Z)-h.Position).Magnitude
        if dist < 1 then
            if autoRightPhase==1 then autoRightPhase=2; return end
            hum:Move(Vector3.zero,false); h.AssemblyLinearVelocity=Vector3.new(0,0,0)
            AutoRightEnabled=false; Enabled.AutoRightEnabled=false
            if VisualSetters and VisualSetters.AutoRightEnabled then VisualSetters.AutoRightEnabled(false,true) end
            if autoRightConnection then autoRightConnection:Disconnect(); autoRightConnection=nil end
            if onAutoRightDone then onAutoRightDone() end
            faceNorth(); return
        end
        local dir=(target-h.Position); local md=Vector3.new(dir.X,0,dir.Z).Unit
        hum:Move(md,false); h.AssemblyLinearVelocity=Vector3.new(md.X*Values.BoostSpeed,h.AssemblyLinearVelocity.Y,md.Z*Values.BoostSpeed)
    end)
end
local function stopAutoRight()
    if autoRightConnection then autoRightConnection:Disconnect(); autoRightConnection=nil end
    autoRightPhase=1
    local c=Player.Character if c then local hum=c:FindFirstChildOfClass("Humanoid") if hum then hum:Move(Vector3.zero,false) end end
end
-- ============================================================
-- FULL AUTO DUEL BACKEND
-- ============================================================
local fadWaypoints = {}
local fadCurrent = 1
local fadMoving = false
local fadWaiting = false
local fadGrabDone = false
local fadMoveConn = nil
local fadSpeedConn = nil
local function fadStop()
    if fadMoveConn then fadMoveConn:Disconnect(); fadMoveConn=nil end
    if fadSpeedConn then fadSpeedConn:Disconnect(); fadSpeedConn=nil end
    fadMoving = false
    fadWaiting = false
    fadGrabDone= false
    Enabled.FullAutoDuel = false
    if VisualSetters and VisualSetters.FullAutoDuel then VisualSetters.FullAutoDuel(false, true) end
end
local function fadMoveLoop()
    if fadMoveConn then fadMoveConn:Disconnect() end
    fadMoveConn = RunService.Stepped:Connect(function()
        if not fadMoving or fadWaiting then return end
        local c = Player.Character if not c then return end
        local root = c:FindFirstChild("HumanoidRootPart") if not root then return end
        local wp = fadWaypoints[fadCurrent]
        local dist = (root.Position - wp.position).Magnitude
        if dist < 5 then
            if (fadCurrent == 4 or fadCurrent == 6) and not fadGrabDone then
                fadWaiting = true
                root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0)
                local c2 = Player.Character if c2 then local h=c2:FindFirstChildOfClass("Humanoid") if h then h:Move(Vector3.zero,false) end end
                return
            end
            if fadCurrent == #fadWaypoints then
                fadStop(); return
            end
            fadCurrent += 1
        else
            local dir = (wp.position - root.Position)
            local md = Vector3.new(dir.X, 0, dir.Z).Unit
            local hum = c:FindFirstChildOfClass("Humanoid")
            if hum then hum:Move(md, false) end
            root.AssemblyLinearVelocity = Vector3.new(md.X*wp.speed, root.AssemblyLinearVelocity.Y, md.Z*wp.speed)
        end
    end)
end
local function startFullAutoDuel()
    local root = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    fadMoving = true
    fadGrabDone = false
    fadCurrent = 1
    if (root.Position - Vector3.new(-475,-7,96)).Magnitude > (root.Position - Vector3.new(-474,-7,23)).Magnitude then
        fadWaypoints = {
            {position=Vector3.new(-475,-7,96), speed=59},
            {position=Vector3.new(-483,-5,95), speed=59},
            {position=Vector3.new(-487,-5,95), speed=55},
            {position=Vector3.new(-492,-5,95), speed=55},
            {position=Vector3.new(-473,-7,95), speed=29},
            {position=Vector3.new(-473,-7,11), speed=29},
        }
    else
        fadWaypoints = {
            {position=Vector3.new(-474,-7,23), speed=55},
            {position=Vector3.new(-484,-5,24), speed=55},
            {position=Vector3.new(-488,-5,24), speed=55},
            {position=Vector3.new(-493,-5,25), speed=55},
            {position=Vector3.new(-473,-7,25), speed=29},
            {position=Vector3.new(-474,-7,112), speed=29},
        }
    end
    if fadSpeedConn then fadSpeedConn:Disconnect() end
    fadSpeedConn = RunService.Heartbeat:Connect(function()
        if not fadWaiting or fadGrabDone then return end
        local c = Player.Character if not c then return end
        local hum = c:FindFirstChildOfClass("Humanoid") if not hum then return end
        if hum.WalkSpeed < 23 then
            task.wait(0.3)
            fadWaiting = false
            fadGrabDone = true
            if fadCurrent < #fadWaypoints then fadCurrent += 1 end
        end
    end)
    fadMoveLoop()
end
local function stopFullAutoDuel()
    fadStop()
end
local function startAntiRagdoll()
    if Connections.antiRagdoll then return end
    Connections.antiRagdoll = RunService.Heartbeat:Connect(function()
        if not Enabled.AntiRagdoll then return end
        local char=Player.Character if not char then return end
        local root=char:FindFirstChild("HumanoidRootPart")
        local hum=char:FindFirstChildOfClass("Humanoid")
        if hum then
            local s=hum:GetState()
            if s==Enum.HumanoidStateType.Physics or s==Enum.HumanoidStateType.Ragdoll or s==Enum.HumanoidStateType.FallingDown then
                hum:ChangeState(Enum.HumanoidStateType.Running)
                workspace.CurrentCamera.CameraSubject=hum
                pcall(function()
                    local pm=Player.PlayerScripts:FindFirstChild("PlayerModule")
                    if pm then require(pm:FindFirstChild("ControlModule")):Enable() end
                end)
                if root then root.Velocity=Vector3.zero; root.RotVelocity=Vector3.zero end
            end
        end
        for _,obj in ipairs(char:GetDescendants()) do
            if obj:IsA("Motor6D") and not obj.Enabled then obj.Enabled=true end
        end
    end)
end
local function stopAntiRagdoll()
    if Connections.antiRagdoll then Connections.antiRagdoll:Disconnect(); Connections.antiRagdoll=nil end
end
--nagiontop
local function startSpeedWhileStealing()
    if Connections.speedWhileStealing then return end
    Connections.speedWhileStealing = RunService.Heartbeat:Connect(function()
        if not Enabled.SpeedWhileStealing or not Player:GetAttribute("Stealing") then return end
        local c=Player.Character if not c then return end
        local h=c:FindFirstChild("HumanoidRootPart") if not h then return end
        local md=getMovementDirection()
        if md.Magnitude > 0.1 then
            h.AssemblyLinearVelocity=Vector3.new(md.X*Values.StealingSpeedValue,h.AssemblyLinearVelocity.Y,md.Z*Values.StealingSpeedValue)
        end
    end)
end
local function stopSpeedWhileStealing()
    if Connections.speedWhileStealing then Connections.speedWhileStealing:Disconnect(); Connections.speedWhileStealing=nil end
end
-- AUTO GRAB
local ProgressBarFill, ProgressPercentLabel
local isStealing = false
local stealStartTime = nil
local progressConnection = nil
local StealData = {}
local function isMyPlotByName(pn)
    local plots=workspace:FindFirstChild("Plots") if not plots then return false end
    local plot=plots:FindFirstChild(pn) if not plot then return false end
    local sign=plot:FindFirstChild("PlotSign")
    if sign then
        local yb=sign:FindFirstChild("YourBase")
        if yb and yb:IsA("BillboardGui") then return yb.Enabled==true end
    end
    return false
end
local function findNearestPrompt()
    local h=Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if not h then return nil end
    local plots=workspace:FindFirstChild("Plots") if not plots then return nil end
    local np,nd,nn=nil,math.huge,nil
    for _,plot in ipairs(plots:GetChildren()) do
        if isMyPlotByName(plot.Name) then continue end
        local podiums=plot:FindFirstChild("AnimalPodiums") if not podiums then continue end
        for _,pod in ipairs(podiums:GetChildren()) do
            pcall(function()
                local base=pod:FindFirstChild("Base")
                local spawn=base and base:FindFirstChild("Spawn")
                if spawn then
                    local dist=(spawn.Position-h.Position).Magnitude
                    if dist<nd and dist<=Values.STEAL_RADIUS then
                        local att=spawn:FindFirstChild("PromptAttachment")
                        if att then
                            for _,ch in ipairs(att:GetChildren()) do
                                if ch:IsA("ProximityPrompt") then np,nd,nn=ch,dist,pod.Name; break end
                            end
                        end
                    end
                end
            end)
        end
    end
    return np,nd,nn
end
local function ResetProgressBar()
    if ProgressPercentLabel then ProgressPercentLabel.Text="0%" end
    if ProgressBarFill then ProgressBarFill.Size=UDim2.new(0,0,1,0) end
end
local function executeSteal(prompt,name)
    if isStealing then return end
    if not StealData[prompt] then
        StealData[prompt]={hold={},trigger={},ready=true}
        pcall(function()
            if getconnections then
                for _,c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do
                    if c.Function then table.insert(StealData[prompt].hold,c.Function) end
                end
                for _,c in ipairs(getconnections(prompt.Triggered)) do
                    if c.Function then table.insert(StealData[prompt].trigger,c.Function) end
                end
            end
        end)
    end
    local data=StealData[prompt]
    if not data.ready then return end
    data.ready=false; isStealing=true; stealStartTime=tick()
    if progressConnection then progressConnection:Disconnect() end
    progressConnection=RunService.Heartbeat:Connect(function()
        if not isStealing then progressConnection:Disconnect(); return end
        local prog=math.clamp((tick()-stealStartTime)/Values.STEAL_DURATION,0,1)
        if ProgressBarFill then ProgressBarFill.Size=UDim2.new(prog,0,1,0) end
        if ProgressPercentLabel then ProgressPercentLabel.Text=math.floor(prog*100).."%" end
    end)
    task.spawn(function()
        for _,f in ipairs(data.hold) do task.spawn(f) end
        task.wait(Values.STEAL_DURATION)
        for _,f in ipairs(data.trigger) do task.spawn(f) end
        if progressConnection then progressConnection:Disconnect() end
        ResetProgressBar(); data.ready=true; task.wait(0.3); isStealing=false
    end)
end
local function startAutoSteal()
    if Connections.autoSteal then return end
    Connections.autoSteal=RunService.Heartbeat:Connect(function()
        if not Enabled.AutoSteal or isStealing then return end
        local p,_,n=findNearestPrompt()
        if p then executeSteal(p,n) end
    end)
end
local function stopAutoSteal()
    if Connections.autoSteal then Connections.autoSteal:Disconnect(); Connections.autoSteal=nil end
    isStealing=false; ResetProgressBar()
end
local savedAnimations={}
local function startUnwalk()
    local c=Player.Character if not c then return end
    local hum=c:FindFirstChildOfClass("Humanoid")
    if hum then for _,t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop() end end
    local anim=c:FindFirstChild("Animate")
    if anim then savedAnimations.Animate=anim:Clone(); anim:Destroy() end
end
local function stopUnwalk()
    local c=Player.Character
    if c and savedAnimations.Animate then savedAnimations.Animate:Clone().Parent=c; savedAnimations.Animate=nil end
end
-- ============================================================
-- OPTIMIZER + XRAY CON AMBIENTE SCURO
-- ============================================================
local originalTransparency = {}
local xrayEnabled = false
local originalBrightness = Lighting.Brightness
local originalAmbient = Lighting.Ambient
local originalOutdoorAmbient = Lighting.OutdoorAmbient
local originalColorShift_Top = Lighting.ColorShift_Top
local originalColorShift_Bottom = Lighting.ColorShift_Bottom
local originalGlobalShadows = Lighting.GlobalShadows
local function enableOptimizer()
    if getgenv and getgenv().OPTIMIZER_ACTIVE then return end
    if getgenv then getgenv().OPTIMIZER_ACTIVE = true end
   
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
    end)
   
    pcall(function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            pcall(function()
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
                    obj:Destroy()
                elseif obj:IsA("BasePart") then
                    obj.CastShadow = false
                    obj.Material = Enum.Material.Plastic
                end
            end)
        end
    end)
   
    pcall(function()
        for _, p in ipairs(Players:GetPlayers()) do
            local char = p.Character
            if char then
                for _, obj in ipairs(char:GetChildren()) do
                    pcall(function()
                        if obj:IsA("Accessory") or obj:IsA("Hat") or obj:IsA("ShirtGraphic") then
                            obj:Destroy()
                        elseif obj:IsA("Shirt") or obj:IsA("Pants") then
                            obj:Destroy()
                        elseif obj:IsA("SpecialMesh") then
                            obj:Destroy()
                        end
                    end)
                end
                for _, obj in ipairs(char:GetDescendants()) do
                    pcall(function()
                        if obj:IsA("SpecialMesh") and obj.Parent and obj.Parent:IsA("BasePart") then
                            local pname = obj.Parent.Name
                            if pname ~= "HumanoidRootPart" then
                                obj.MeshId = ""
                                obj.TextureId = ""
                            end
                        end
                        if obj:IsA("Decal") or obj:IsA("Texture") then
                            obj:Destroy()
                        end
                    end)
                end
            end
        end
    end)
   
    xrayEnabled = true
    pcall(function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and obj.Anchored and (obj.Name:lower():find("base") or (obj.Parent and obj.Parent.Name:lower():find("base"))) then
                originalTransparency[obj] = obj.LocalTransparencyModifier
                obj.LocalTransparencyModifier = 0.85
            end
        end
    end)
end
local function disableOptimizer()
local abActive = false
local _abMoveConn, _abNdConn, _abNdHumConn, _abTarget
local abVisualSetter = nil
local function _abApplyNoDie(char)
    if _abNdConn then _abNdConn:Disconnect(); _abNdConn = nil end
    if _abNdHumConn then _abNdHumConn:Disconnect(); _abNdHumConn = nil end
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    pcall(function() hum.MaxHealth = math.huge end)
    pcall(function() hum.Health = math.huge end)
    _abNdHumConn = hum:GetPropertyChangedSignal("Health"):Connect(function()
        if not abActive then return end
        pcall(function() if hum.Health < 1 then hum.Health = hum.MaxHealth end end)
    end)
    _abNdConn = RunService.Heartbeat:Connect(function()
        if not abActive then return end
        pcall(function()
            if not hum or not hum.Parent then return end
            if hum.Health < hum.MaxHealth then hum.Health = hum.MaxHealth end
            local s = hum:GetState()
            if s == Enum.HumanoidStateType.Dead or s == Enum.HumanoidStateType.Ragdoll or s == Enum.HumanoidStateType.FallingDown then
                hum:ChangeState(Enum.HumanoidStateType.Running)
            end
        end)
    end)
end
local function _abStop()
    if _abMoveConn then _abMoveConn:Disconnect(); _abMoveConn = nil end
    if _abNdConn then _abNdConn:Disconnect(); _abNdConn = nil end
    if _abNdHumConn then _abNdHumConn:Disconnect(); _abNdHumConn = nil end
    _abTarget = nil
    pcall(function()
        local hum = Player.Character and Player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.MaxHealth = 100; hum:Move(Vector3.zero, false) end
    end)
end
local function _abStart()
    _abStop()
    local char = Player.Character
    if char then _abApplyNoDie(char) end
    _abMoveConn = RunService.Heartbeat:Connect(function()
        if not abActive then return end
        pcall(function()
            local myChar = Player.Character if not myChar then return end
            local myHRP = myChar:FindFirstChild("HumanoidRootPart")
            local myHum = myChar:FindFirstChildOfClass("Humanoid")
            if not myHRP or not myHum then return end
            local nearest, bestDist = nil, math.huge
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= Player and p.Character then
                    local oh = p.Character:FindFirstChild("HumanoidRootPart")
                    if oh then
                        local d = (myHRP.Position - oh.Position).Magnitude
                        if d < bestDist then bestDist = d; nearest = p end
                    end
                end
            end
            _abTarget = nearest
            if not _abTarget or not _abTarget.Character then return end
            local tHRP = _abTarget.Character:FindFirstChild("HumanoidRootPart")
            if not tHRP then return end
            local flat = Vector3.new((tHRP.Position - myHRP.Position).X, 0, (tHRP.Position - myHRP.Position).Z)
            if flat.Magnitude < 3 then myHum:Move(Vector3.zero, false); return end
            local unit = flat.Unit
            myHum:Move(unit, false)
            myHRP.AssemblyLinearVelocity = Vector3.new(unit.X * Values.BoostSpeed, myHRP.AssemblyLinearVelocity.Y, unit.Z * Values.BoostSpeed)
            pcall(function() myHRP.CFrame = CFrame.new(myHRP.Position, myHRP.Position + flat) end)
        end)
    end)
end
local function abToggle()
    abActive = not abActive
    if abActive then _abStart() else _abStop() end
    if abVisualSetter then abVisualSetter(abActive) end
end
Player.CharacterAdded:Connect(function(nc)
    if not abActive then return end
    task.wait(0.5)
    _abApplyNoDie(nc)
    _abStart()
end)
    if getgenv then getgenv().OPTIMIZER_ACTIVE = false end
   
    pcall(function()
        Lighting.Brightness = originalBrightness
        Lighting.Ambient = originalAmbient
        Lighting.OutdoorAmbient = originalOutdoorAmbient
        Lighting.ColorShift_Top = originalColorShift_Top
        Lighting.ColorShift_Bottom = originalColorShift_Bottom
        Lighting.GlobalShadows = originalGlobalShadows
        settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
    end)
   
    if xrayEnabled then
        for part, v in pairs(originalTransparency) do
            if part then
                part.LocalTransparencyModifier = v
            end
        end
        originalTransparency = {}
        xrayEnabled = false
    end
end
local function _abApplyNoDie(char)
    if _abNdConn then _abNdConn:Disconnect(); _abNdConn=nil end
    if _abNdHumConn then _abNdHumConn:Disconnect(); _abNdHumConn=nil end
    local hum=char and char:FindFirstChildOfClass("Humanoid") if not hum then return end
    pcall(function() hum.MaxHealth=math.huge end); pcall(function() hum.Health=math.huge end)
    _abNdHumConn=hum:GetPropertyChangedSignal("Health"):Connect(function()
        if not abActive then return end
        pcall(function() if hum.Health<1 then hum.Health=hum.MaxHealth end end)
    end)
    _abNdConn=RunService.Heartbeat:Connect(function()
        if not abActive then return end
        pcall(function()
            if not hum or not hum.Parent then return end
            if hum.Health<hum.MaxHealth then hum.Health=hum.MaxHealth end
            local s=hum:GetState()
            if s==Enum.HumanoidStateType.Dead or s==Enum.HumanoidStateType.Ragdoll or s==Enum.HumanoidStateType.FallingDown then
                hum:ChangeState(Enum.HumanoidStateType.Running)
            end
        end)
    end)
end
local function _abStop()
    if _abMoveConn then _abMoveConn:Disconnect(); _abMoveConn=nil end
    if _abNdConn then _abNdConn:Disconnect(); _abNdConn=nil end
    if _abNdHumConn then _abNdHumConn:Disconnect(); _abNdHumConn=nil end
    _abTarget=nil
    pcall(function()
        local hum=Player.Character and Player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.MaxHealth=100; hum:Move(Vector3.zero,false) end
    end)
end
local function _abStart()
    _abStop()
    local char=Player.Character if char then _abApplyNoDie(char) end
    _abMoveConn=RunService.Heartbeat:Connect(function()
        if not abActive then return end
        pcall(function()
            local myChar=Player.Character if not myChar then return end
            local myHRP=myChar:FindFirstChild("HumanoidRootPart")
            local myHum=myChar:FindFirstChildOfClass("Humanoid")
            if not myHRP or not myHum then return end
            local nearest,bestDist=nil,math.huge
            for _,p in ipairs(Players:GetPlayers()) do
                if p~=Player and p.Character then
                    local oh=p.Character:FindFirstChild("HumanoidRootPart")
                    if oh then local d=(myHRP.Position-oh.Position).Magnitude if d<bestDist then bestDist=d; nearest=p end end
                end
            end
            _abTarget=nearest
            if not _abTarget or not _abTarget.Character then return end
            local tHRP=_abTarget.Character:FindFirstChild("HumanoidRootPart") if not tHRP then return end
            local flat=Vector3.new((tHRP.Position-myHRP.Position).X,0,(tHRP.Position-myHRP.Position).Z)
            if flat.Magnitude<3 then myHum:Move(Vector3.zero,false); return end
            local unit=flat.Unit; myHum:Move(unit,false)
            myHRP.AssemblyLinearVelocity=Vector3.new(unit.X*Values.BoostSpeed,myHRP.AssemblyLinearVelocity.Y,unit.Z*Values.BoostSpeed)
            pcall(function() myHRP.CFrame=CFrame.new(myHRP.Position,myHRP.Position+flat) end)
        end)
    end)
end
local function abToggle()
    abActive=not abActive
    if abActive then _abStart() else _abStop() end
    if abVisualSetter then abVisualSetter(abActive) end
end
Player.CharacterAdded:Connect(function(nc)
    if not abActive then return end
    task.wait(0.5); _abApplyNoDie(nc); _abStart()
end)
-- ============================================================
-- SPEED LABEL ABOVE CHARACTER
-- ============================================================
local speedBillboard = nil
local speedTextLabel = nil
local function createSpeedLabel(char)
    if speedBillboard then speedBillboard:Destroy(); speedBillboard=nil; speedTextLabel=nil end
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local bb = Instance.new("BillboardGui")
    bb.Name = "Nagi_SpeedLabel"; bb.AlwaysOnTop = false
    bb.Size = UDim2.new(0, 150, 0, 40); bb.StudsOffset = Vector3.new(0, 3.2, 0)
    bb.MaxDistance = 60; bb.Parent = hrp
    local lbl = Instance.new("TextLabel", bb)
    lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1
    lbl.Text = "Speed: 0.0"; lbl.TextColor3 = Color3.fromRGB(0, 220, 255)
    lbl.TextStrokeColor3 = Color3.fromRGB(0, 80, 120); lbl.TextStrokeTransparency = 0.01
    lbl.Font = Enum.Font.GothamBlack; lbl.TextSize = 45
    lbl.TextXAlignment = Enum.TextXAlignment.Center
    speedBillboard = bb; speedTextLabel = lbl
end
task.spawn(function() task.wait(1); createSpeedLabel(Player.Character) end)
Player.CharacterAdded:Connect(function(char) task.wait(0.5); createSpeedLabel(char) end)
RunService.Heartbeat:Connect(function()
    if not speedTextLabel then return end
    pcall(function()
        local char = Player.Character if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart") if not hrp then return end
        local vel = hrp.AssemblyLinearVelocity
        local horizontalSpeed = Vector3.new(vel.X, 0, vel.Z).Magnitude
        speedTextLabel.Text = "Speed: " .. string.format("%.1f", horizontalSpeed)
    end)
end)
-- ============================================================
-- GUI - STILE COMPATTO COME IMMAGINE
-- ============================================================
local isMobile = UserInputService.TouchEnabled
local GS = isMobile and 0.35 or 1 -- 35% della dimensione normale (molto piccola)
local function onTap(btn, cb)
    if isMobile then
        btn.Activated:Connect(cb)
    else
        btn.MouseButton1Click:Connect(cb)
    end
end
local C = {
    bg           = Color3.fromRGB(18, 18, 22),
    bgRow        = Color3.fromRGB(28, 28, 35),
    white        = Color3.fromRGB(235, 235, 240),
    dim          = Color3.fromRGB(130, 130, 148),
    muted        = Color3.fromRGB(90, 90, 110),
    off          = Color3.fromRGB(38, 38, 48),
    accent       = Color3.fromRGB(255, 75, 75),
    accentGold   = Color3.fromRGB(200, 165, 90),
    buttonBg     = Color3.fromRGB(38, 38, 48),
    buttonBorder = Color3.fromRGB(55, 55, 68),
    badge        = Color3.fromRGB(38, 38, 48),
    badgeActive  = Color3.fromRGB(80, 80, 100),
    neonBorder   = Color3.fromRGB(55, 55, 68),
    sliderBoost  = Color3.fromRGB(255, 255, 255),
    sliderGravity= Color3.fromRGB(200, 165, 90),
    danger       = Color3.fromRGB(190, 65, 65),
    success      = Color3.fromRGB(65, 170, 90),
}
local WIN_W = isMobile and 220 or math.floor(370*GS)
local WIN_H = isMobile and 480 or math.floor(650*GS)
local CR = isMobile and 6 or 8
local sg = Instance.new("ScreenGui")
sg.Name="ZAY_CLEAN"; sg.ResetOnSpawn=false
sg.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
sg.Parent=Player.PlayerGui
local function playSound(id,vol,spd)
    pcall(function()
        local s=Instance.new("Sound",SoundService)
        s.SoundId=id; s.Volume=vol or 0.3; s.PlaybackSpeed=spd or 1
        s:Play(); game:GetService("Debris"):AddItem(s,1)
    end)
end
local function rc(inst, r)
    local c = Instance.new("UICorner", inst); c.CornerRadius = UDim.new(0, r or CR)
end
local function st(inst, thick, col, trans)
    local s = Instance.new("UIStroke", inst)
    s.Thickness=thick or 2; s.Color=col or C.white; s.Transparency=trans or 0
    s.ApplyStrokeMode=Enum.ApplyStrokeMode.Border
end
local main = Instance.new("Frame", sg)
main.Name = "ZAY_Main"
main.Size = UDim2.new(0, WIN_W, 0, WIN_H)
main.Position = isMobile and UDim2.new(0.5, -WIN_W/2, 0.15, 0) or UDim2.new(1, -(WIN_W+16), 0, 16)main.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
main.BackgroundTransparency = 0.08 -- ← Cambiato qui: 0 = totalmente opaco
main.BorderSizePixel = 0
main.Active = true
main.Draggable = not isMobile
rc(main, CR)
main.ClipsDescendants = true
st(main, 1, Color3.fromRGB(55, 55, 68), 0)
if isMobile then
    local mobDragging = false
    local mobDragStart, mobFrameStart
    local titleHitbox = Instance.new("TextButton", main)
    titleHitbox.Size = UDim2.new(1,0,0,32)
    titleHitbox.Position = UDim2.new(0,0,0,0)
    titleHitbox.BackgroundTransparency = 1; titleHitbox.Text = ""
    titleHitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch then
            mobDragging = true
            mobDragStart = inp.Position
            mobFrameStart = main.Position
        end
    end)
-- ============================================================
-- BOTTONE MINIMIZZA VIOLA (VERSIONE COMPATTA)
-- ============================================================
local minimizeSimple = Instance.new("TextButton")
minimizeSimple.Size = UDim2.new(0, 28, 0, 28)
minimizeSimple.Position = UDim2.new(1, -68, 0, 6) -- accanto alla X (che sta a -36)
minimizeSimple.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
minimizeSimple.Text = "−"
minimizeSimple.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeSimple.Font = Enum.Font.GothamBold
minimizeSimple.TextSize = 20
minimizeSimple.BorderSizePixel = 0
minimizeSimple.Parent = main
local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(0, 8)
minimizeCorner.Parent = minimizeSimple
local minimized = false
local originalHeight = main.Size.Y.Offset
local contentFrame = scroll -- lo scrolling frame
minimizeSimple.MouseButton1Click:Connect(function()
    minimized = not minimized
   
    local targetHeight = minimized and 38 or originalHeight
    local targetTrans = minimized and 1 or 0
   
    TweenService:Create(main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
        Size = UDim2.new(0, WIN_W, 0, targetHeight)
    }):Play()
   
    TweenService:Create(contentFrame, TweenInfo.new(0.25), {
        BackgroundTransparency = targetTrans
    }):Play()
   
    minimizeSimple.Text = minimized and "+" or "−"
   
    if minimized then
        contentFrame.Visible = false
    else
        contentFrame.Visible = true
    end
   
    playSound("rbxassetid://6895079813", 0.3, minimized and 0.9 or 1.1)
end)
minimizeSimple.MouseEnter:Connect(function()
    TweenService:Create(minimizeSimple, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(170, 70, 240)
    }):Play()
end)
minimizeSimple.MouseLeave:Connect(function()
    TweenService:Create(minimizeSimple, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    }):Play()
end)
    titleHitbox.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch then mobDragging = false end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if mobDragging and inp.UserInputType == Enum.UserInputType.Touch then
            local delta = inp.Position - mobDragStart
            main.Position = UDim2.new(
                mobFrameStart.X.Scale, mobFrameStart.X.Offset + delta.X,
                mobFrameStart.Y.Scale, mobFrameStart.Y.Offset + delta.Y
            )
        end
    end)
end
-- BARRA BIANCA IN ALTO A SINISTRA
-- BARRA PICCOLA ARROTONDATA AFFIANCO AL TITOLO
-- BARRA PICCOLA ARROTONDATA AFFIANCO AL TITOLO (main gui)
local whiteBar = Instance.new("Frame", main)
whiteBar.Size = UDim2.new(0, 3, 0, 16)
whiteBar.Position = UDim2.new(0, 10, 0, 8)
whiteBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
whiteBar.BorderSizePixel = 0
local whiteBarCorner = Instance.new("UICorner", whiteBar)
whiteBarCorner.CornerRadius = UDim.new(0, 4)
-- TITOLO PICCOLO
local titleTxt = Instance.new("TextLabel", main)
titleTxt.Size=UDim2.new(1,0,0,32); titleTxt.Position=UDim2.new(0,18,0,0)
titleTxt.BackgroundTransparency=1; titleTxt.Text="Vortex Hub"; titleTxt.TextColor3=C.white
titleTxt.Font=Enum.Font.GothamBlack; titleTxt.TextSize=math.floor(14*GS)
titleTxt.TextXAlignment=Enum.TextXAlignment.Left; titleTxt.TextYAlignment=Enum.TextYAlignment.Center
-- VERSIONE
local versionTxt = Instance.new("TextLabel", main)
versionTxt.Size=UDim2.new(0,60,0,32); versionTxt.Position=UDim2.new(1,-70,0,0)
versionTxt.BackgroundTransparency=1; versionTxt.Text="v8.34"; versionTxt.TextColor3=C.dim
versionTxt.Font=Enum.Font.GothamMedium; versionTxt.TextSize=math.floor(10*GS)
versionTxt.TextXAlignment=Enum.TextXAlignment.Right; versionTxt.TextYAlignment=Enum.TextYAlignment.Center
-- PREMIUM LABEL
local premiumTxt = Instance.new("TextLabel", main)
premiumTxt.Size=UDim2.new(0,70,0,32); premiumTxt.Position=UDim2.new(1,-140,0,0)
premiumTxt.BackgroundTransparency=1; premiumTxt.Text="Premium v1"; premiumTxt.TextColor3=C.dim
premiumTxt.Font=Enum.Font.GothamMedium; premiumTxt.TextSize=math.floor(10*GS)
premiumTxt.TextXAlignment=Enum.TextXAlignment.Right; premiumTxt.TextYAlignment=Enum.TextYAlignment.Center
local divider = Instance.new("Frame", main)
divider.Size=UDim2.new(1,-16,0,1); divider.Position=UDim2.new(0,8,0,32)
divider.BackgroundColor3=C.accent; divider.BorderSizePixel=0
local SCROLL_Y = 38
local scroll = Instance.new("ScrollingFrame", main)
scroll.Size=UDim2.new(1,0,0,WIN_H-SCROLL_Y-8); scroll.Position=UDim2.new(0,0,0,SCROLL_Y)
scroll.BackgroundTransparency=1; scroll.BorderSizePixel=0
scroll.ScrollBarThickness=3; scroll.ScrollBarImageColor3=C.dim
scroll.CanvasSize=UDim2.new(0,0,0,0); scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y
scroll.ScrollingDirection=Enum.ScrollingDirection.Y
-- PALLINI DOPO SCROLL E DIVIDER
local CBW = math.floor(12*GS)
local DOT_GAP = 24
local dotsFrame = Instance.new("Frame", main)
dotsFrame.Size = UDim2.new(0, DOT_GAP*2 + CBW*3, 0, 32)
dotsFrame.Position = UDim2.new(0.5, -((DOT_GAP*2 + CBW*3)/2), 0, 0)
dotsFrame.BackgroundTransparency = 1
dotsFrame.BorderSizePixel = 0
dotsFrame.Active = false
dotsFrame.ZIndex = 20
local closeBtn = Instance.new("TextButton", dotsFrame)
closeBtn.Size = UDim2.new(0, CBW, 0, CBW)
closeBtn.Position = UDim2.new(0, 0, 0.5, -CBW/2)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 75, 75)
closeBtn.Text = ""; closeBtn.BorderSizePixel = 0; closeBtn.ZIndex = 20
rc(closeBtn, CBW/2)
closeBtn.MouseEnter:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 120, 120)}):Play()
end)
closeBtn.MouseLeave:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 75, 75)}):Play()
end)
local minimizeBtn = Instance.new("TextButton", dotsFrame)
minimizeBtn.Size = UDim2.new(0, CBW, 0, CBW)
minimizeBtn.Position = UDim2.new(0, DOT_GAP, 0.5, -CBW/2)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 190, 0)
minimizeBtn.Text = ""; minimizeBtn.BorderSizePixel = 0; minimizeBtn.ZIndex = 20
rc(minimizeBtn, CBW/2)
minimizeBtn.MouseEnter:Connect(function()
    TweenService:Create(minimizeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 220, 80)}):Play()
end)
minimizeBtn.MouseLeave:Connect(function()
    TweenService:Create(minimizeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 190, 0)}):Play()
end)
local greenDot = Instance.new("Frame", dotsFrame)
greenDot.Size = UDim2.new(0, CBW, 0, CBW)
greenDot.Position = UDim2.new(0, DOT_GAP*2, 0.5, -CBW/2)
greenDot.BackgroundColor3 = Color3.fromRGB(50, 210, 100)
greenDot.BorderSizePixel = 0; greenDot.ZIndex = 20
rc(greenDot, CBW/2)
local minimized = false
local originalHeight = WIN_H
minimizeBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    if minimized then
        scroll.Visible = false
        divider.Visible = false
        TweenService:Create(main, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, WIN_W, 0, 34)
        }):Play()
    else
        scroll.Visible = true
        divider.Visible = true
        TweenService:Create(main, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, WIN_W, 0, originalHeight)
        }):Play()
    end
    playSound("rbxassetid://6895079813", 0.3, minimized and 0.9 or 1.1)
end)
scroll.Size=UDim2.new(1,0,0,WIN_H-SCROLL_Y-8); scroll.Position=UDim2.new(0,0,0,SCROLL_Y)
scroll.BackgroundTransparency=1; scroll.BorderSizePixel=0
scroll.ScrollBarThickness=3; scroll.ScrollBarImageColor3=C.dim
scroll.CanvasSize=UDim2.new(0,0,0,0); scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y
scroll.ScrollingDirection=Enum.ScrollingDirection.Y
local layout = Instance.new("UIListLayout", scroll)
layout.SortOrder=Enum.SortOrder.LayoutOrder; layout.Padding=UDim.new(0,4)
local pad = Instance.new("UIPadding", scroll)
pad.PaddingTop=UDim.new(0,6); pad.PaddingBottom=UDim.new(0,6)
pad.PaddingLeft=UDim.new(0,6); pad.PaddingRight=UDim.new(0,6)
VisualSetters = {}
local SliderSetters = {}
local waitingForKeybind = nil
local KeyBindBtns = {}
local ROW_H = isMobile and 26 or 34
local SL_H = isMobile and 38 or 48
local function createSectionHeader(order, txt)
    local w = Instance.new("Frame", scroll)
    w.Size = UDim2.new(1, 0, 0, 22)
    w.BackgroundTransparency = 1
    w.LayoutOrder = order
    local lineLeft = Instance.new("Frame", w)
    lineLeft.Size = UDim2.new(0.5, -40, 0, 1)
    lineLeft.Position = UDim2.new(0, 0, 0.5, 0)
    lineLeft.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
    lineLeft.BackgroundTransparency = 0.4
    lineLeft.BorderSizePixel = 0
    local lbl = Instance.new("TextLabel", w)
    lbl.Size = UDim2.new(0, 80, 1, 0)
    lbl.Position = UDim2.new(0.5, -40, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = txt
    lbl.TextColor3 = Color3.fromRGB(100, 200, 255)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Center
    local lineRight = Instance.new("Frame", w)
    lineRight.Size = UDim2.new(0.5, -40, 0, 1)
    lineRight.Position = UDim2.new(0.5, 40, 0.5, 0)
    lineRight.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
    lineRight.BackgroundTransparency = 0.4
    lineRight.BorderSizePixel = 0
end
local KB_W = 44
local KB_H = 20
local function createKeybindBadge(parent, keybindKey)
    if not keybindKey then return nil end
    local kc0 = KEYBINDS[keybindKey]
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0, KB_W, 0, KB_H)
    btn.Position = UDim2.new(1, -(KB_W + 6), 0.5, -KB_H/2)
    btn.BackgroundColor3 = C.badge
    btn.Text = (kc0 == Enum.KeyCode.Unknown) and "—" or kc0.Name
    btn.TextColor3 = C.white
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 9
    btn.BorderSizePixel = 0
    rc(btn, 4)
    st(btn, 1, C.dim, 0.4)
    KeyBindBtns[keybindKey] = btn
    btn.MouseButton1Click:Connect(function()
        if waitingForKeybind == keybindKey then
            waitingForKeybind = nil
            local kc2 = KEYBINDS[keybindKey]
            btn.Text = (kc2 == Enum.KeyCode.Unknown) and "—" or kc2.Name
            TweenService:Create(btn, TweenInfo.new(0.15), {
                BackgroundColor3 = armedKeybinds[keybindKey] and C.accent or C.badge
            }):Play()
            return
        end
        if KEYBINDS[keybindKey] ~= Enum.KeyCode.Unknown then
            armedKeybinds[keybindKey] = not armedKeybinds[keybindKey]
            local armed = armedKeybinds[keybindKey]
            TweenService:Create(btn, TweenInfo.new(0.15), {
                BackgroundColor3 = armed and C.accent or C.badge
            }):Play()
            playSound("rbxassetid://6895079813", 0.3, armed and 1.2 or 0.8)
            return
        end
        if waitingForKeybind then
            local prev = KeyBindBtns[waitingForKeybind]
            if prev then
                local pkc = KEYBINDS[waitingForKeybind]
                prev.Text = (pkc == Enum.KeyCode.Unknown) and "—" or pkc.Name
                TweenService:Create(prev, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
            end
        end
        waitingForKeybind = keybindKey
        btn.Text = "?"
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = C.badgeActive}):Play()
        playSound("rbxassetid://6895079813", 0.3, 1.5)
    end)
    btn.MouseButton2Click:Connect(function()
        KEYBINDS[keybindKey] = Enum.KeyCode.Unknown
        btn.Text = "—"
        waitingForKeybind = nil
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
    end)
    return btn
end
local function createToggle(order, labelTxt, enabledKey, callback, _unused, keybindKey)
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1,0,0,ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = order
    -- Angoli rotondi tipo cilindro
    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 5)
    -- Bordo neon viola chiarissimo
    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 0.7
    stroke.Color = C.neonBorder
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local PW = 38
    local rightReserve = 8 + PW + (keybindKey and (6 + KB_W) or 0) + 8
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -(8 + rightReserve), 1, 0)
    lbl.Position = UDim2.new(0,8,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelTxt
    lbl.TextColor3 = C.dim
    lbl.Font = Enum.Font.GothamSemibold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    local isOn = Enabled[enabledKey] or false
    -- Pill toggle
    local pill = Instance.new("Frame", row)
    pill.Size = UDim2.new(0, PW, 0, 22)
    pill.Position = UDim2.new(1, -(PW+8), 0.5, -11)
    pill.BackgroundColor3 = isOn and C.accent or C.off
    pill.BorderSizePixel = 0
    local pillCorner = Instance.new("UICorner", pill)
    pillCorner.CornerRadius = UDim.new(0, 11)
    local knob = Instance.new("Frame", pill)
    knob.Size = UDim2.new(0, 16, 0, 16)
    knob.Position = isOn and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
    knob.BackgroundColor3 = isOn and C.white or C.dim
    knob.BorderSizePixel = 0
    local knobCorner = Instance.new("UICorner", knob)
    knobCorner.CornerRadius = UDim.new(0, 8)
    -- Keybind badge
    if keybindKey then
        local kbBtn = Instance.new("TextButton", row)
        kbBtn.Size = UDim2.new(0, KB_W, 0, KB_H)
        kbBtn.Position = UDim2.new(1, -(PW + 8 + 6 + KB_W), 0.5, -KB_H/2)
        kbBtn.BackgroundColor3 = C.badge
        kbBtn.Text = (KEYBINDS[keybindKey] == Enum.KeyCode.Unknown) and "—" or KEYBINDS[keybindKey].Name
        kbBtn.TextColor3 = C.white
        kbBtn.Font = Enum.Font.GothamBold
        kbBtn.TextSize = 9
        kbBtn.BorderSizePixel = 0
        rc(kbBtn, 4)
        st(kbBtn, 1, C.dim, 0.4)
        KeyBindBtns[keybindKey] = kbBtn
        kbBtn.MouseButton1Click:Connect(function()
            if waitingForKeybind == keybindKey then
                waitingForKeybind = nil
                kbBtn.Text = (KEYBINDS[keybindKey] == Enum.KeyCode.Unknown) and "—" or KEYBINDS[keybindKey].Name
                TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
                return
            end
            if waitingForKeybind then
                local prev = KeyBindBtns[waitingForKeybind]
                if prev then
                    local pkc = KEYBINDS[waitingForKeybind]
                    prev.Text = (pkc == Enum.KeyCode.Unknown) and "—" or pkc.Name
                    TweenService:Create(prev, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
                end
            end
            waitingForKeybind = keybindKey
            kbBtn.Text = "?"
            TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = C.badgeActive}):Play()
        end)
        kbBtn.MouseButton2Click:Connect(function()
            KEYBINDS[keybindKey] = Enum.KeyCode.Unknown
            kbBtn.Text = "—"
            waitingForKeybind = nil
            TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
        end)
    end
    local function setVisual(state, skipCB)
        isOn = state
        TweenService:Create(pill, TweenInfo.new(0.18), {BackgroundColor3 = isOn and C.accent or C.off}):Play()
        TweenService:Create(knob, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
            BackgroundColor3 = isOn and C.white or C.dim,
            Position = isOn and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
        }):Play()
        if not skipCB then callback(isOn) end
    end
    VisualSetters[enabledKey] = setVisual
    local pillClk = Instance.new("TextButton", pill)
    pillClk.Size = UDim2.new(1,0,1,0)
    pillClk.BackgroundTransparency = 1
    pillClk.Text = ""
    pillClk.ZIndex = pill.ZIndex + 1
    local function doToggle()
        if waitingForKeybind then return end
        isOn = not isOn
        Enabled[enabledKey] = isOn
        setVisual(isOn)
        playSound("rbxassetid://6895079813", 0.3, isOn and 1.1 or 0.9)
    end
    onTap(pillClk, doToggle)
    local clk = Instance.new("TextButton", row)
    local clkWidth = keybindKey and -(PW + 8 + 6 + KB_W + 8) or -(PW + 16)
    clk.Size = UDim2.new(1, clkWidth, 1, 0)
    clk.BackgroundTransparency = 1
    clk.Text = ""
    onTap(clk, doToggle)
    return row, setVisual
end
local function createSlider(order, labelTxt, minV, maxV, valueKey, callback, customColor)
    local f = Instance.new("Frame", scroll)
    f.Size=UDim2.new(1,0,0,SL_H); f.BackgroundColor3=C.bgRow
    f.BorderSizePixel=0; f.LayoutOrder=order
    rc(f, 6); st(f, 0.8, C.border, 0.4)
    local lbl = Instance.new("TextLabel", f)
    lbl.Size=UDim2.new(0.6,0,0,16); lbl.Position=UDim2.new(0,8,0,4)
    lbl.BackgroundTransparency=1; lbl.Text=labelTxt; lbl.TextColor3=C.dim
    lbl.Font=Enum.Font.GothamMedium; lbl.TextSize=10
    local function r1(n) return math.floor(n*10+0.5)/10 end
    local function fmt(n) return string.format("%.1f",n) end
    local defVal = r1(Values[valueKey] or minV)
    local valBox = Instance.new("TextBox", f)
    valBox.Size=UDim2.new(0,46,0,18); valBox.Position=UDim2.new(1,-52,0,2)
    valBox.BackgroundColor3=C.off; valBox.Text=fmt(defVal); valBox.TextColor3=C.white
    valBox.Font=Enum.Font.GothamBold; valBox.TextSize=10
    valBox.ClearTextOnFocus=false; valBox.BorderSizePixel=0
    rc(valBox, 4)
    local track = Instance.new("Frame", f)
    track.Size=UDim2.new(1,-16,0,4); track.Position=UDim2.new(0,8,1,-12)
    track.BackgroundColor3=C.off; track.BorderSizePixel=0
    rc(track, 2)
    -- Usa colore personalizzato o default viola
    local sliderColor = customColor or C.accent
   
    local pct = math.clamp((defVal-minV)/(maxV-minV), 0, 1)
    local fill = Instance.new("Frame", track)
    fill.Size=UDim2.new(pct,0,1,0); fill.BackgroundColor3=sliderColor; fill.BorderSizePixel=0; rc(fill, 2)
    local thumb = Instance.new("Frame", track)
    thumb.Size=UDim2.new(0,12,0,12); thumb.Position=UDim2.new(pct,-6,0.5,-6)
    thumb.BackgroundColor3=sliderColor; thumb.BorderSizePixel=0
    rc(thumb, 6); st(thumb, 1, C.white, 0)
    local hitbox = Instance.new("TextButton", track)
    hitbox.Size=UDim2.new(1,0,5,0); hitbox.Position=UDim2.new(0,0,-2,0)
    hitbox.BackgroundTransparency=1; hitbox.Text=""
    local dragging = false
    local function setVal(rel)
        rel=math.clamp(rel,0,1)
        fill.Size=UDim2.new(rel,0,1,0)
        thumb.Position=UDim2.new(rel,-6,0.5,-6)
        local v=r1(math.clamp(r1(minV+(maxV-minV)*rel),minV,maxV))
        valBox.Text=fmt(v); Values[valueKey]=v; callback(v)
    end
    local function setSlider(v)
        v=r1(math.clamp(v,minV,maxV)); local r=(v-minV)/(maxV-minV)
        fill.Size=UDim2.new(r,0,1,0); thumb.Position=UDim2.new(r,-6,0.5,-6)
        valBox.Text=fmt(v); Values[valueKey]=v
    end
    SliderSetters[valueKey] = setSlider
    hitbox.MouseButton1Down:Connect(function() dragging=true end)
    hitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.Touch then dragging=true end
    end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then dragging=false end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if dragging and (inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch) then
            setVal((inp.Position.X-track.AbsolutePosition.X)/track.AbsoluteSize.X)
        end
    end)
    valBox.FocusLost:Connect(function()
        local n=tonumber(valBox.Text)
        if n then setSlider(r1(math.clamp(n,minV,maxV))); callback(Values[valueKey])
        else valBox.Text=fmt(Values[valueKey] or minV) end
    end)
    return f, setSlider
end
local function createButtonRow(order, txt, bgCol, strokeCol, callback)
    local f = Instance.new("Frame", scroll)
    f.Size = UDim2.new(1,0,0,38)
    f.BackgroundTransparency = 1
    f.LayoutOrder = order
    local btn = Instance.new("TextButton", f)
    btn.Size = UDim2.new(1,0,1,0)
    btn.BackgroundColor3 = Color3.fromRGB(20, 24, 36)
    btn.BackgroundTransparency = 0.35 -- ← Meno trasparente (0.35 è buono)
    btn.Text = txt
    btn.TextColor3 = C.white
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.BorderSizePixel = 0
    -- Angoli molto rotondi (effetto cilindro)
    local corner = Instance.new("UICorner", btn)
    corner.CornerRadius = UDim.new(0, 9)
    -- Bordo più spesso e di colore diverso
    local stroke = Instance.new("UIStroke", btn)
    stroke.Thickness = 1 -- ← Bordi più spessi
    stroke.Color = Color3.fromRGB(55, 55, 68)
    stroke.Transparency = 0.3
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    onTap(btn, function() callback(btn) end)
    return f, btn
end
local PB_W = isMobile and math.floor(WIN_W * 0.95) or math.floor(340*GS)
local PB_H = isMobile and 38 or math.floor(44*GS)
local progBar = Instance.new("Frame", sg)
progBar.Size = UDim2.new(0, PB_W, 0, PB_H)
progBar.Position = isMobile and UDim2.new(0.5, -PB_W/2, 1, -50) or UDim2.new(0.5, -PB_W/2, 0.85, -PB_H/2)
progBar.BackgroundColor3 = C.bg
progBar.BorderSizePixel = 0
rc(progBar, isMobile and 10 or 20)
st(progBar, 1, C.accent, 0)

ProgressPercentLabel = Instance.new("TextLabel", progBar)
ProgressPercentLabel.Size = UDim2.new(0, 40, 0, 20)
ProgressPercentLabel.Position = UDim2.new(0, 6, 0, 2)
ProgressPercentLabel.BackgroundTransparency = 1
ProgressPercentLabel.Text = "0%"
ProgressPercentLabel.TextColor3 = C.white
ProgressPercentLabel.Font = Enum.Font.GothamBlack
ProgressPercentLabel.TextSize = isMobile and 12 or 16
ProgressPercentLabel.TextXAlignment = Enum.TextXAlignment.Left
ProgressPercentLabel.TextYAlignment = Enum.TextYAlignment.Center

local vortexText = Instance.new("TextLabel", progBar)
vortexText.Size = UDim2.new(0, 70, 0, 24)
vortexText.Position = UDim2.new(0.5, -35, 0, 2)
vortexText.BackgroundTransparency = 1
vortexText.Text = "VORTEX"
vortexText.TextColor3 = Color3.fromRGB(0, 200, 255)
vortexText.Font = Enum.Font.Garamond
vortexText.TextSize = isMobile and 13 or 18
vortexText.TextXAlignment = Enum.TextXAlignment.Center
vortexText.TextYAlignment = Enum.TextYAlignment.Center

local IW = isMobile and 32 or 38
local IH = isMobile and 20 or 24

local function makePBInput(xOffset, labelStr, valueKey, isFloat, minV, maxV)
    local holder = Instance.new("Frame", progBar)
    holder.Size = UDim2.new(0, IW, 0, IH)
    holder.Position = UDim2.new(1, -(IW + xOffset), 0, 2)
    holder.BackgroundColor3 = C.bgRow
    holder.BorderSizePixel = 0
    rc(holder, 4)
    local lbl = Instance.new("TextLabel", holder)
    lbl.Size = UDim2.new(1, 0, 0, 9)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelStr
    lbl.TextColor3 = C.muted
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 6
    lbl.TextXAlignment = Enum.TextXAlignment.Center
    local box = Instance.new("TextBox", holder)
    box.Size = UDim2.new(1, -4, 0, 11)
    box.Position = UDim2.new(0, 2, 0, 9)
    box.BackgroundTransparency = 1
    box.Text = tostring(Values[valueKey])
    box.TextColor3 = C.white
    box.Font = Enum.Font.GothamBold
    box.TextSize = isMobile and 8 or 9
    box.ClearTextOnFocus = false
    box.BorderSizePixel = 0
    box.TextXAlignment = Enum.TextXAlignment.Center
    box.FocusLost:Connect(function()
        local n = tonumber(box.Text)
        if n then
            if isFloat then n = math.clamp(math.floor(n*100+0.5)/100, minV, maxV)
            else n = math.clamp(math.floor(n), minV, maxV) end
            Values[valueKey] = n; box.Text = tostring(n); SaveConfig()
        else box.Text = tostring(Values[valueKey]) end
    end)
end

makePBInput(4, "RADIUS", "STEAL_RADIUS", false, 5, 100)
makePBInput(4 + IW + 4, "SPEED", "STEAL_DURATION", true, 0.05, 5)

local pbTrack = Instance.new("Frame", progBar)
pbTrack.Size = UDim2.new(1, -12, 0, 5)
pbTrack.Position = UDim2.new(0, 6, 1, -8)
pbTrack.BackgroundColor3 = C.off
pbTrack.BorderSizePixel = 0
rc(pbTrack, 2)

ProgressBarFill = Instance.new("Frame", pbTrack)
ProgressBarFill.Size = UDim2.new(0, 0, 1, 0)
ProgressBarFill.BackgroundColor3 = C.accent
ProgressBarFill.BorderSizePixel = 0
rc(ProgressBarFill, 2)
-- ============================================================
-- BUILD ALL ROWS
-- ============================================================
local o = 0
-- ============================================================
-- SEZIONE MODE
-- ============================================================
createSectionHeader(o, "MODE") o += 1

local activeModeBtn = nil
local modeDefs = {
    {
        label = "Desync Mode",
        color = Color3.fromRGB(255, 80, 80),
        speed = 50.8,
        stealSpeed = 25,
        autoSpeed = 50.8,
        autoReturnSpeed = 25,
    },
    {
        label = "AutoPlay Mode",
        color = Color3.fromRGB(100, 200, 255),
        speed = 60,
        stealSpeed = 30,
        autoSpeed = 61.2,
        autoReturnSpeed = 30,
    },
    {
        label = "Lagback Mode",
        color = Color3.fromRGB(180, 80, 255),
        speed = 38,
        stealSpeed = 14.3,
        autoSpeed = 38,
        autoReturnSpeed = 14.3,
    },
}

for _, def in ipairs(modeDefs) do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1, 0, 0, ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o

    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)

    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = def.color
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -(8 + 50 + 8), 1, 0)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = def.label
    lbl.TextColor3 = def.color
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local modeBtn = Instance.new("TextButton", row)
    modeBtn.Size = UDim2.new(0, 50, 0, 22)
    modeBtn.Position = UDim2.new(1, -58, 0.5, -11)
    modeBtn.BackgroundColor3 = C.off
    modeBtn.Text = "OFF"
    modeBtn.TextColor3 = C.white
    modeBtn.Font = Enum.Font.GothamBold
    modeBtn.TextSize = 10
    modeBtn.BorderSizePixel = 0

    local btnCorner = Instance.new("UICorner", modeBtn)
    btnCorner.CornerRadius = UDim.new(0, 8)

    local btnStroke = Instance.new("UIStroke", modeBtn)
    btnStroke.Thickness = 1
    btnStroke.Color = def.color
    btnStroke.Transparency = 0.3
    btnStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local isActive = false

    local function deactivate()
        isActive = false
        modeBtn.Text = "OFF"
        TweenService:Create(modeBtn, TweenInfo.new(0.15), {
            BackgroundColor3 = C.off
        }):Play()
        TweenService:Create(stroke, TweenInfo.new(0.15), {
            Transparency = 0.35
        }):Play()
    end

    local function activate()
        -- Disattiva il precedente se esiste
        if activeModeBtn and activeModeBtn ~= deactivate then
            activeModeBtn()
        end
        isActive = true
        activeModeBtn = deactivate
        modeBtn.Text = "ON"
        TweenService:Create(modeBtn, TweenInfo.new(0.15), {
            BackgroundColor3 = def.color
        }):Play()
        TweenService:Create(stroke, TweenInfo.new(0.15), {
            Transparency = 0.0
        }):Play()
        -- Applica valori
        Values.BoostSpeed = def.speed
        Values.StealingSpeedValue = def.stealSpeed
        if SliderSetters.BoostSpeed then SliderSetters.BoostSpeed(def.speed) end
        if SliderSetters.StealingSpeedValue then SliderSetters.StealingSpeedValue(def.stealSpeed) end
        SPEED_IDA3 = def.autoSpeed
        SPEED_VOLTA3 = def.autoReturnSpeed
        SPEED_IDA4 = def.autoSpeed
        SPEED_VOLTA4 = def.autoReturnSpeed
        playSound("rbxassetid://6895079813", 0.3, 1.1)
    end

    onTap(modeBtn, function()
        if isActive then
            deactivate()
            activeModeBtn = nil
        else
            activate()
        end
    end)

    local clk = Instance.new("TextButton", row)
    clk.Size = UDim2.new(1, -(50 + 16), 1, 0)
    clk.BackgroundTransparency = 1
    clk.Text = ""
    onTap(clk, function()
        if isActive then
            deactivate()
            activeModeBtn = nil
        else
            activate()
        end
    end)

    o += 1
end
createToggle(o,"Speed Boost","SpeedBoost",
    function(s) Enabled.SpeedBoost=s; if s then startSpeedBoost() else stopSpeedBoost() end end,
    nil,"SPEED") o+=1
createSlider(o,"Boost Speed",1,70,"BoostSpeed",function(v) Values.BoostSpeed=v end, Color3.fromRGB(100, 200, 255)) o+=1
createToggle(o,"Speed Steal","SpeedWhileStealing",
    function(s) Enabled.SpeedWhileStealing=s; if s then startSpeedWhileStealing() else stopSpeedWhileStealing() end end,
    nil, nil) o+=1
createSlider(o,"Steal Speed",10,35,"StealingSpeedValue",function(v) Values.StealingSpeedValue=v end) o+=1
createToggle(o,"Boosters (Galaxy)","Galaxy",
    function(s) Enabled.Galaxy=s; if s then startGalaxy() else stopGalaxy() end end,
    nil,"GALAXY") o+=1
createSlider(o,"Gravity %",25,130,"GalaxyGravityPercent",
    function(v) Values.GalaxyGravityPercent=v; if galaxyEnabled then adjustGalaxyJump() end end, Color3.fromRGB(255, 140, 60)) o+=1
createSlider(o,"Jump Boost",10,80,"HOP_POWER",function(v) Values.HOP_POWER=v end) o+=1
createToggle(o,"Anti Ragdoll","AntiRagdoll",
    function(s) Enabled.AntiRagdoll=s; if s then startAntiRagdoll() else stopAntiRagdoll() end end,
    nil,"ANTIRAGDOLL") o+=1
createToggle(o,"Spin Bot","SpinBot",
    function(s) Enabled.SpinBot=s; if s then startSpinBot() else stopSpinBot() end end,
    nil,"SPIN") o+=1
createSlider(o,"Spin Speed",5,50,"SpinSpeed",function(v) Values.SpinSpeed=v end) o+=1
createSectionHeader(o,"COMBAT") o+=1
createToggle(o,"Instant Grab","AutoSteal",
    function(s) Enabled.AutoSteal=s; if s then startAutoSteal() else stopAutoSteal() end end,
    nil, nil) o+=1
createSlider(o,"Steal Radius",5,100,"STEAL_RADIUS",function(v) Values.STEAL_RADIUS=v end) o+=1
createSectionHeader(o,"MISC") o+=1
createToggle(o,"Optimizer + XRay","Optimizer",
    function(s) Enabled.Optimizer=s; if s then enableOptimizer() else disableOptimizer() end end,
    nil, nil) o+=1
createToggle(o,"Spam Bat","SpamBat",
    function(s)
        Enabled.SpamBat=s
        if s then
            startSpamBat(); startSpamBatCircle()
        else
            stopSpamBat(); removeSpamBatCircle()
        end
    end,
    nil, nil) o+=1
do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1,0,0,ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o
    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)
    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = Color3.fromRGB(255, 140, 60) -- arancione solo qui
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local PW = 38
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -(8 + 8 + PW + 8), 1, 0)
    lbl.Position = UDim2.new(0,8,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Map FOV"
    lbl.TextColor3 = C.dim
    lbl.Font = Enum.Font.GothamSemibold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    local pill = Instance.new("Frame", row)
    pill.Size = UDim2.new(0, PW, 0, 22)
    pill.Position = UDim2.new(1, -(PW+8), 0.5, -11)
    pill.BackgroundColor3 = C.off
    pill.BorderSizePixel = 0
    local pillCorner = Instance.new("UICorner", pill)
    pillCorner.CornerRadius = UDim.new(0, 11)
    local knob = Instance.new("Frame", pill)
    knob.Size = UDim2.new(0, 16, 0, 16)
    knob.Position = UDim2.new(0, 3, 0.5, -8)
    knob.BackgroundColor3 = C.dim
    knob.BorderSizePixel = 0
    local knobCorner = Instance.new("UICorner", knob)
    knobCorner.CornerRadius = UDim.new(0, 8)
    local isOn = false
    local function doToggle()
        isOn = not isOn
        Enabled.MapFOV = isOn
        TweenService:Create(pill, TweenInfo.new(0.18), {BackgroundColor3 = isOn and Color3.fromRGB(255, 140, 60) or C.off}):Play()
        TweenService:Create(knob, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
            BackgroundColor3 = isOn and C.white or C.dim,
            Position = isOn and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
        }):Play()
        playSound("rbxassetid://6895079813", 0.3, isOn and 1.1 or 0.9)
    end
    local pillClk = Instance.new("TextButton", pill)
    pillClk.Size = UDim2.new(1,0,1,0)
    pillClk.BackgroundTransparency = 1
    pillClk.Text = ""
    pillClk.ZIndex = pill.ZIndex + 1
    onTap(pillClk, doToggle)
    local clk = Instance.new("TextButton", row)
    clk.Size = UDim2.new(1, -(PW+16), 1, 0)
    clk.BackgroundTransparency = 1
    clk.Text = ""
    onTap(clk, doToggle)
end
o+=1
createSlider(o,"FOV",30,120,"FOV",function(v)
    Values.FOV=v
    if Enabled.MapFOV then
        pcall(function() local cam=workspace.CurrentCamera; if cam then cam.FieldOfView=v end end)
    end
end, Color3.fromRGB(255, 140, 60)) o+=1
-- VORTEX HUB ON TOP - DIRECT SEND BUTTON
do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1, 0, 0, ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o
    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)
    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = Color3.fromRGB(138, 43, 226)
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -80, 1, 0)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Vortex Hub On Top"
    lbl.TextColor3 = Color3.fromRGB(138, 43, 226)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    local sendBtn = Instance.new("TextButton", row)
    sendBtn.Size = UDim2.new(0, 50, 0, 22)
    sendBtn.Position = UDim2.new(1, -58, 0.5, -11)
    sendBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    sendBtn.Text = "Send"
    sendBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    sendBtn.Font = Enum.Font.GothamBold
    sendBtn.TextSize = 10
    sendBtn.BorderSizePixel = 0
    local sendCorner = Instance.new("UICorner", sendBtn)
    sendCorner.CornerRadius = UDim.new(0, 8)
    -- Hover effect
    sendBtn.MouseEnter:Connect(function()
        TweenService:Create(sendBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(170, 70, 240)}):Play()
    end)
    sendBtn.MouseLeave:Connect(function()
        TweenService:Create(sendBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(138, 43, 226)}):Play()
    end)
    -- DIRECT SEND - No GUI popup
    sendBtn.MouseButton1Click:Connect(function()
        playSound("rbxassetid://6895079813", 0.3, 1.1)
       
        local TextChatService = game:GetService("TextChatService")
        if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local channel = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if channel then
                channel:SendAsync("Vortex On HubTop XD")
            end
        else
            local SayMessageRequest = ReplicatedStorage:WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest")
            SayMessageRequest:FireServer("Vortex On Top XD", "All")
        end
        -- Visual feedback
        sendBtn.Text = "Sent!"
        sendBtn.BackgroundColor3 = Color3.fromRGB(100, 255, 120)
       
        task.delay(1, function()
            sendBtn.Text = "Send"
            sendBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
        end)
    end)
end
o += 1
createSectionHeader(o,"KEYBINDS") o+=1
local keybindDefs = {
    { label = "Anti Ragdoll", key = "ANTIRAGDOLL" },
    { label = "Galaxy Mode", key = "GALAXY" },
    { label = "Auto Grab ( In Developing...)", key = "AUTOSTEAL" },
    { label = "Optimizer + XRay", key = "OPTIMIZER" },
    { label = "Speed Boost", key = "SPEED" },
    { label = "Speed While Stealing", key = "SPEEDSTEAL" },
}
if not KEYBINDS.OPTIMIZER then KEYBINDS.OPTIMIZER = Enum.KeyCode.Unknown end
if not KEYBINDS.SPEEDSTEAL then KEYBINDS.SPEEDSTEAL = Enum.KeyCode.Unknown end
if not KEYBINDS.AUTOSTEAL then KEYBINDS.AUTOSTEAL = Enum.KeyCode.Unknown end
for _, def in ipairs(keybindDefs) do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1, 0, 0, ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o
    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)
    local isAutoLock = def.key == "AUTOSTEAL"
    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = isAutoLock and Color3.fromRGB(255, 140, 30) or C.neonBorder
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -(8 + KB_W + 16), 1, 0)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = def.label
    lbl.TextColor3 = isAutoLock and Color3.fromRGB(255, 140, 30) or C.dim
    lbl.Font = Enum.Font.GothamSemibold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    local kbBtn = Instance.new("TextButton", row)
    kbBtn.Size = UDim2.new(0, KB_W, 0, KB_H)
    kbBtn.Position = UDim2.new(1, -(KB_W + 8), 0.5, -KB_H / 2)
    kbBtn.BackgroundColor3 = C.badge
    kbBtn.Text = (KEYBINDS[def.key] == Enum.KeyCode.Unknown) and "—" or KEYBINDS[def.key].Name
    kbBtn.TextColor3 = C.white
    kbBtn.Font = Enum.Font.GothamBold
    kbBtn.TextSize = 9
    kbBtn.BorderSizePixel = 0
    rc(kbBtn, 4)
    st(kbBtn, 1, C.dim, 0.4)
    KeyBindBtns[def.key] = kbBtn
    kbBtn.MouseButton1Click:Connect(function()
        if waitingForKeybind == def.key then
            waitingForKeybind = nil
            kbBtn.Text = (KEYBINDS[def.key] == Enum.KeyCode.Unknown) and "—" or KEYBINDS[def.key].Name
            TweenService:Create(kbBtn, TweenInfo.new(0.15), { BackgroundColor3 = C.badge }):Play()
            return
        end
        if waitingForKeybind then
            local prev = KeyBindBtns[waitingForKeybind]
            if prev then
                local pkc = KEYBINDS[waitingForKeybind]
                prev.Text = (pkc == Enum.KeyCode.Unknown) and "—" or pkc.Name
                TweenService:Create(prev, TweenInfo.new(0.15), { BackgroundColor3 = C.badge }):Play()
            end
        end
        waitingForKeybind = def.key
        kbBtn.Text = "?"
        TweenService:Create(kbBtn, TweenInfo.new(0.15), { BackgroundColor3 = C.badgeActive }):Play()
        playSound("rbxassetid://6895079813", 0.3, 1.5)
    end)
    kbBtn.MouseButton2Click:Connect(function()
        KEYBINDS[def.key] = Enum.KeyCode.Unknown
        kbBtn.Text = "—"
        waitingForKeybind = nil
        TweenService:Create(kbBtn, TweenInfo.new(0.15), { BackgroundColor3 = C.badge }):Play()
    end)
    o += 1
end  -- chiude il for loop keybindDefs

createSectionHeader(o,"SETTINGS") o+=1

-- SLIDER TRASPARENZA GUI (REDESIGN)
do
    local SL_H2 = 56
    local f = Instance.new("Frame", scroll)
    f.Size = UDim2.new(1,0,0,SL_H2)
    f.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    f.BackgroundTransparency = 0
    f.BorderSizePixel = 0
    f.LayoutOrder = o
    rc(f, 10)
    local fStroke = Instance.new("UIStroke", f)
    fStroke.Thickness = 1; fStroke.Color = C.accent; fStroke.Transparency = 0.6
    fStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    -- Icona 👁
    local icon = Instance.new("TextLabel", f)
    icon.Size = UDim2.new(0, 20, 0, 20)
    icon.Position = UDim2.new(0, 8, 0, 8)
    icon.BackgroundTransparency = 1
    icon.Text = ""
    icon.TextSize = 14
    icon.Font = Enum.Font.GothamBold

    local lbl = Instance.new("TextLabel", f)
    lbl.Size = UDim2.new(0.5, 0, 0, 18)
    lbl.Position = UDim2.new(0, 30, 0, 6)
    lbl.BackgroundTransparency = 1
    lbl.Text = "GUI Opacity"
    lbl.TextColor3 = C.white
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local valBox = Instance.new("Frame", f)
    valBox.Size = UDim2.new(0, 42, 0, 18)
    valBox.Position = UDim2.new(1, -50, 0, 6)
    valBox.BackgroundColor3 = C.accent
    valBox.BackgroundTransparency = 0.7
    valBox.BorderSizePixel = 0
    rc(valBox, 6)
    local valLbl = Instance.new("TextLabel", valBox)
    valLbl.Size = UDim2.new(1,0,1,0)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = "100%"
    valLbl.TextColor3 = C.white
    valLbl.Font = Enum.Font.GothamBlack
    valLbl.TextSize = 10
    valLbl.TextXAlignment = Enum.TextXAlignment.Center

    local trackBg = Instance.new("Frame", f)
    trackBg.Size = UDim2.new(1,-16,0,6)
    trackBg.Position = UDim2.new(0,8,1,-14)
    trackBg.BackgroundColor3 = Color3.fromRGB(30,30,45)
    trackBg.BorderSizePixel = 0
    rc(trackBg, 3)

    local fill = Instance.new("Frame", trackBg)
    fill.Size = UDim2.new(1,0,1,0)
    fill.BackgroundColor3 = C.accent
    fill.BorderSizePixel = 0
    rc(fill, 3)

    local grd = Instance.new("UIGradient", fill)
    grd.Color = ColorSequence.new(C.accent, Color3.fromRGB(0,120,200))
    grd.Rotation = 0

    local thumb = Instance.new("Frame", trackBg)
    thumb.Size = UDim2.new(0,14,0,14)
    thumb.Position = UDim2.new(1,-7,0.5,-7)
    thumb.BackgroundColor3 = C.white
    thumb.BorderSizePixel = 0
    rc(thumb, 7)
    local tShadow = Instance.new("UIStroke", thumb)
    tShadow.Thickness = 2; tShadow.Color = C.accent; tShadow.Transparency = 0.3

    local hitbox = Instance.new("TextButton", trackBg)
    hitbox.Size = UDim2.new(1,0,5,0); hitbox.Position = UDim2.new(0,0,-2,0)
    hitbox.BackgroundTransparency = 1; hitbox.Text = ""

    local dragOp = false
    local function setOpacity(rel)
        rel = math.clamp(rel, 0, 1)
        fill.Size = UDim2.new(rel, 0, 1, 0)
        thumb.Position = UDim2.new(rel, -7, 0.5, -7)
        local pct = math.floor(rel * 100 + 0.5)
        valLbl.Text = pct .. "%"
        main.BackgroundTransparency = 1 - rel
    end

    hitbox.MouseButton1Down:Connect(function() dragOp = true end)
    hitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch then dragOp = true end
    end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then dragOp = false end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if dragOp and (inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch) then
            setOpacity((inp.Position.X - trackBg.AbsolutePosition.X) / trackBg.AbsoluteSize.X)
        end
    end)
end
o += 1

-- SLIDER SCALA GUI (REDESIGN)
do
    local SL_H2 = 56
    local f = Instance.new("Frame", scroll)
    f.Size = UDim2.new(1,0,0,SL_H2)
    f.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    f.BackgroundTransparency = 0
    f.BorderSizePixel = 0
    f.LayoutOrder = o
    rc(f, 10)
    local fStroke = Instance.new("UIStroke", f)
    fStroke.Thickness = 1; fStroke.Color = Color3.fromRGB(138,43,226); fStroke.Transparency = 0.6
    fStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    -- Icona ⊞
    local icon = Instance.new("TextLabel", f)
    icon.Size = UDim2.new(0, 20, 0, 20)
    icon.Position = UDim2.new(0, 8, 0, 8)
    icon.BackgroundTransparency = 1
    icon.Text = ""
    icon.TextSize = 14
    icon.Font = Enum.Font.GothamBold

    local lbl = Instance.new("TextLabel", f)
    lbl.Size = UDim2.new(0.5, 0, 0, 18)
    lbl.Position = UDim2.new(0, 30, 0, 6)
    lbl.BackgroundTransparency = 1
    lbl.Text = "GUI Scale"
    lbl.TextColor3 = C.white
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local valBox = Instance.new("Frame", f)
    valBox.Size = UDim2.new(0, 42, 0, 18)
    valBox.Position = UDim2.new(1, -50, 0, 6)
    valBox.BackgroundColor3 = Color3.fromRGB(138,43,226)
    valBox.BackgroundTransparency = 0.7
    valBox.BorderSizePixel = 0
    rc(valBox, 6)
    local valLbl = Instance.new("TextLabel", valBox)
    valLbl.Size = UDim2.new(1,0,1,0)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = "100%"
    valLbl.TextColor3 = C.white
    valLbl.Font = Enum.Font.GothamBlack
    valLbl.TextSize = 10
    valLbl.TextXAlignment = Enum.TextXAlignment.Center

    local trackBg = Instance.new("Frame", f)
    trackBg.Size = UDim2.new(1,-16,0,6)
    trackBg.Position = UDim2.new(0,8,1,-14)
    trackBg.BackgroundColor3 = Color3.fromRGB(30,30,45)
    trackBg.BorderSizePixel = 0
    rc(trackBg, 3)

    local defPct = 0.5
    local fill = Instance.new("Frame", trackBg)
    fill.Size = UDim2.new(defPct,0,1,0)
    fill.BackgroundColor3 = Color3.fromRGB(138,43,226)
    fill.BorderSizePixel = 0
    rc(fill, 3)

    local grd = Instance.new("UIGradient", fill)
    grd.Color = ColorSequence.new(Color3.fromRGB(138,43,226), Color3.fromRGB(200,80,255))
    grd.Rotation = 0

    local thumb = Instance.new("Frame", trackBg)
    thumb.Size = UDim2.new(0,14,0,14)
    thumb.Position = UDim2.new(defPct,-7,0.5,-7)
    thumb.BackgroundColor3 = C.white
    thumb.BorderSizePixel = 0
    rc(thumb, 7)
    local tShadow = Instance.new("UIStroke", thumb)
    tShadow.Thickness = 2; tShadow.Color = Color3.fromRGB(138,43,226); tShadow.Transparency = 0.3

    local hitbox = Instance.new("TextButton", trackBg)
    hitbox.Size = UDim2.new(1,0,5,0); hitbox.Position = UDim2.new(0,0,-2,0)
    hitbox.BackgroundTransparency = 1; hitbox.Text = ""

    local dragSc = false
    local baseW = WIN_W
    local baseH = WIN_H

    local function setScale(rel)
        rel = math.clamp(rel, 0, 1)
        fill.Size = UDim2.new(rel, 0, 1, 0)
        thumb.Position = UDim2.new(rel, -7, 0.5, -7)
        local scale = 0.5 + rel * 1.0
        local pct = math.floor(scale * 100 + 0.5)
        valLbl.Text = pct .. "%"
        local newW = math.floor(baseW * scale)
        local newH = math.floor(baseH * scale)
        main.Size = UDim2.new(0, newW, 0, newH)
    end

    hitbox.MouseButton1Down:Connect(function() dragSc = true end)
    hitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch then dragSc = true end
    end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then dragSc = false end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if dragSc and (inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch) then
            setScale((inp.Position.X - trackBg.AbsolutePosition.X) / trackBg.AbsoluteSize.X)
        end
    end)
end
o += 1

-- AUTO SAVE TOGGLE
local autoSaveEnabled = false
local autoSaveConn = nil
createToggle(o, "Auto Save (10s)", "AutoSaveToggle",
    function(s)
        autoSaveEnabled = s
        if s then
            autoSaveConn = task.spawn(function()
                while autoSaveEnabled do
                    task.wait(10)
                    if autoSaveEnabled then SaveConfig() end
                end
            end)
        else
            autoSaveEnabled = false
        end
    end,
    nil, nil) o+=1

-- VORTEX DISCORD COPY
do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1, 0, 0, ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o

    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)

    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = Color3.fromRGB(0, 200, 255)
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local discordIcon = Instance.new("ImageLabel", row)
    discordIcon.Size = UDim2.new(0, 18, 0, 18)
    discordIcon.Position = UDim2.new(0, 8, 0.5, -9)
    discordIcon.BackgroundTransparency = 1
    discordIcon.Image = "rbxassetid://88649111237347"
    
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -80, 1, 0)
    lbl.Position = UDim2.new(0, 30, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Vortex Discord"
    lbl.TextColor3 = Color3.fromRGB(0, 200, 255)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local copyBtn = Instance.new("TextButton", row)
    copyBtn.Size = UDim2.new(0, 50, 0, 22)
    copyBtn.Position = UDim2.new(1, -58, 0.5, -11)
    copyBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 230)
    copyBtn.Text = "Copy"
    copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.TextSize = 10
    copyBtn.BorderSizePixel = 0
    local copyCorner = Instance.new("UICorner", copyBtn)
    copyCorner.CornerRadius = UDim.new(0, 8)

    copyBtn.MouseEnter:Connect(function()
        TweenService:Create(copyBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0, 220, 255)}):Play()
    end)
    copyBtn.MouseLeave:Connect(function()
        TweenService:Create(copyBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0, 180, 230)}):Play()
    end)

    copyBtn.MouseButton1Click:Connect(function()
        pcall(function() setclipboard("https://discord.gg/TpeuntSm") end)
        copyBtn.Text = "✓"
        copyBtn.BackgroundColor3 = Color3.fromRGB(50, 210, 100)
        task.delay(1.5, function()
            copyBtn.Text = "Copy"
            copyBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 230)
        end)
        playSound("rbxassetid://6895079813", 0.3, 1.2)
    end)
end
o += 1
-- VORTEX TIKTOK
do
    local row = Instance.new("Frame", scroll)
    row.Size = UDim2.new(1, 0, 0, ROW_H)
    row.BackgroundColor3 = C.bgRow
    row.BackgroundTransparency = 0.85
    row.BorderSizePixel = 0
    row.LayoutOrder = o
    local corner = Instance.new("UICorner", row)
    corner.CornerRadius = UDim.new(0, 9)
    local stroke = Instance.new("UIStroke", row)
    stroke.Thickness = 1
    stroke.Color = Color3.fromRGB(255, 0, 80)
    stroke.Transparency = 0.35
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local ttkIcon = Instance.new("ImageLabel", row)
    ttkIcon.Size = UDim2.new(0, 18, 0, 18)
    ttkIcon.Position = UDim2.new(0, 8, 0.5, -9)
    ttkIcon.BackgroundTransparency = 1
    ttkIcon.Image = "rbxassetid://119257967099110"

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -80, 1, 0)
    lbl.Position = UDim2.new(0, 30, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Vortex TikTok"
    lbl.TextColor3 = Color3.fromRGB(255, 0, 80)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local copyBtn = Instance.new("TextButton", row)
    copyBtn.Size = UDim2.new(0, 50, 0, 22)
    copyBtn.Position = UDim2.new(1, -58, 0.5, -11)
    copyBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 80)
    copyBtn.Text = "Copy"
    copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.TextSize = 10
    copyBtn.BorderSizePixel = 0
    local copyCorner = Instance.new("UICorner", copyBtn)
    copyCorner.CornerRadius = UDim.new(0, 8)
    copyBtn.MouseButton1Click:Connect(function()
        pcall(function() setclipboard("https://www.tiktok.com/@vortexexploit") end)
        copyBtn.Text = "✓"
        copyBtn.BackgroundColor3 = Color3.fromRGB(50, 210, 100)
        task.delay(1.5, function()
            copyBtn.Text = "Copy"
            copyBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 80)
        end)
        playSound("rbxassetid://6895079813", 0.3, 1.2)
    end)
end
o += 1
createSectionHeader(o,"CONFIG") o+=1
createButtonRow(o," SAVE CONFIG",Color3.fromRGB(70,70,95),C.accent,
    function(btn)
        local ok = SaveConfig()
        btn.Text=ok and "✓ SAVED!" or "✗ FAILED"
        btn.BackgroundColor3=ok and C.off or Color3.fromRGB(100,60,80)
        task.delay(1.5,function() btn.Text="💾 SAVE CONFIG"; btn.BackgroundColor3=Color3.fromRGB(70,70,95) end)
        playSound("rbxassetid://6895079813",0.4,ok and 1.2 or 0.7)
    end) o+=1
createButtonRow(o,"🗑 RESET CONFIG",Color3.fromRGB(90,60,90),Color3.fromRGB(120,70,100),
    function(btn)
        for k in pairs(Enabled) do Enabled[k]=false end
        Values.BoostSpeed=30; Values.SpinSpeed=30; Values.StealingSpeedValue=29
        Values.STEAL_RADIUS=20; Values.STEAL_DURATION=1.3; Values.GalaxyGravityPercent=70
        Values.HOP_POWER=35; Values.FOV=70;
        KEYBINDS.SPEED=Enum.KeyCode.V; KEYBINDS.SPIN=Enum.KeyCode.N; KEYBINDS.GALAXY=Enum.KeyCode.M
        KEYBINDS.AUTOLEFT=Enum.KeyCode.Z; KEYBINDS.AUTORIGHT=Enum.KeyCode.C
        KEYBINDS.ANTIRAGDOLL=Enum.KeyCode.Unknown; KEYBINDS.AIMBOT=Enum.KeyCode.Unknown
        KEYBINDS.FLOAT=Enum.KeyCode.Unknown; KEYBINDS.FULLAUTODUEL=Enum.KeyCode.Unknown
        for _,setter in pairs(VisualSetters) do setter(false,true) end
        for k,setter in pairs(SliderSetters) do if Values[k] then setter(Values[k]) end end
        for k,badgeBtn in pairs(KeyBindBtns) do
            local kc=KEYBINDS[k]
            badgeBtn.Text = (kc==Enum.KeyCode.Unknown) and "—" or kc.Name
        end
        stopSpeedBoost(); stopAntiRagdoll(); stopSpinBot(); stopSpamBat(); removeSpamBatCircle()
        stopAutoSteal(); stopGalaxy(); stopSpeedWhileStealing(); stopUnwalk()
        stopAutoWalk(); stopAutoRight(); disableOptimizer(); stopHitboxExpander(); stopFullAutoDuel()
Enabled.MapFOV=false
        if abActive then abToggle() end
        AutoWalkEnabled=false; AutoRightEnabled=false
        pcall(function() local cam=workspace.CurrentCamera; if cam then cam.FieldOfView=70 end end)
        btn.Text="✓ RESET!"; btn.BackgroundColor3=C.off
        task.delay(1.5,function() btn.Text="🗑 RESET CONFIG"; btn.BackgroundColor3=Color3.fromRGB(90,60,90) end)
        playSound("rbxassetid://6895079813",0.5,0.8)
    end) o+=1
-- ============================================================
-- MINI BUTTON
-- ============================================================
local miniBtn = Instance.new("TextButton", sg)
miniBtn.Size=UDim2.new(0,34,0,34)
miniBtn.Position=UDim2.new(1,-44,0,10)
miniBtn.BackgroundColor3=C.bg; miniBtn.Text="V"; miniBtn.TextColor3=C.white
miniBtn.Font=Enum.Font.GothamBlack; miniBtn.TextSize=16
miniBtn.BorderSizePixel=0; miniBtn.Visible=false
rc(miniBtn, 8); st(miniBtn, 1, C.accent, 0)
onTap(miniBtn, function()
    main.Visible=true; miniBtn.Visible=false; playSound("rbxassetid://6895079813",0.4,1)
end)
onTap(closeBtn, function()
    main.Visible=false; miniBtn.Visible=true; playSound("rbxassetid://6895079813",0.4,1)
end)
-- ============================================================
-- APPLY LOADED CONFIG
-- ============================================================
task.spawn(function()
    task.wait(2.5)
    local c = Player.Character
    if not c or not c:FindFirstChild("HumanoidRootPart") then
        c=Player.CharacterAdded:Wait(); task.wait(1)
    end
    for key,setter in pairs(VisualSetters) do setter(Enabled[key] or false, true) end
    for key,setter in pairs(SliderSetters) do if Values[key] then setter(Values[key]) end end
    for k,badgeBtn in pairs(KeyBindBtns) do
        local kc=KEYBINDS[k]
        badgeBtn.Text = (kc==Enum.KeyCode.Unknown) and "—" or kc.Name
    end
    pcall(function() local cam=workspace.CurrentCamera; if cam and Values.FOV then cam.FieldOfView=Values.FOV end end)
    if Enabled.AntiRagdoll then startAntiRagdoll() end
    if Enabled.AutoSteal then startAutoSteal() end
    if Enabled.Optimizer then enableOptimizer() end
    task.wait(0.3)
    if Enabled.SpeedBoost then startSpeedBoost() end
    if Enabled.SpinBot then startSpinBot() end
    if Enabled.SpamBat then startSpamBat(); startSpamBatCircle() end
    if Enabled.Galaxy then startGalaxy() end
    if Enabled.SpeedWhileStealing then startSpeedWhileStealing() end
    if Enabled.Unwalk then startUnwalk() end
    if Enabled.FullAutoDuel then startFullAutoDuel() end
    if Enabled.Aimbot and not abActive then abToggle() end
end)
-- ============================================================
-- KEYBIND DISPATCH
-- ============================================================
local function dispatchKey(kc)
    for keybindKey, boundKC in pairs(KEYBINDS) do
        if kc == boundKC and boundKC ~= Enum.KeyCode.Unknown then
            if keybindKey == "SPEED" then
                Enabled.SpeedBoost = not Enabled.SpeedBoost
                if VisualSetters.SpeedBoost then VisualSetters.SpeedBoost(Enabled.SpeedBoost) end
                if Enabled.SpeedBoost then startSpeedBoost() else stopSpeedBoost() end
            elseif keybindKey == "SPIN" then
                Enabled.SpinBot = not Enabled.SpinBot
                if VisualSetters.SpinBot then VisualSetters.SpinBot(Enabled.SpinBot) end
                if Enabled.SpinBot then startSpinBot() else stopSpinBot() end
            elseif keybindKey == "GALAXY" then
                Enabled.Galaxy = not Enabled.Galaxy
                if VisualSetters.Galaxy then VisualSetters.Galaxy(Enabled.Galaxy) end
                if Enabled.Galaxy then startGalaxy() else stopGalaxy() end
            elseif keybindKey == "AUTOLEFT" then
                AutoWalkEnabled = not AutoWalkEnabled
                Enabled.AutoWalkEnabled = AutoWalkEnabled
                if VisualSetters.AutoWalkEnabled then VisualSetters.AutoWalkEnabled(AutoWalkEnabled) end
                if AutoWalkEnabled then startAutoWalk() else stopAutoWalk() end
            elseif keybindKey == "AUTORIGHT" then
                AutoRightEnabled = not AutoRightEnabled
                Enabled.AutoRightEnabled = AutoRightEnabled
                if VisualSetters.AutoRightEnabled then VisualSetters.AutoRightEnabled(AutoRightEnabled) end
                if AutoRightEnabled then startAutoRight() else stopAutoRight() end
            elseif keybindKey == "ANTIRAGDOLL" then
                Enabled.AntiRagdoll = not Enabled.AntiRagdoll
                if VisualSetters.AntiRagdoll then VisualSetters.AntiRagdoll(Enabled.AntiRagdoll) end
                if Enabled.AntiRagdoll then startAntiRagdoll() else stopAntiRagdoll() end
            elseif keybindKey == "AIMBOT" then
                abToggle()
            elseif keybindKey == "FLOAT" then
                Enabled.Float = not Enabled.Float
                if VisualSetters.Float then VisualSetters.Float(Enabled.Float) end
                if Enabled.Float then startHitboxExpander() else stopHitboxExpander() end
            elseif keybindKey == "FULLAUTODUEL" then
                Enabled.FullAutoDuel = not Enabled.FullAutoDuel
                if VisualSetters.FullAutoDuel then VisualSetters.FullAutoDuel(Enabled.FullAutoDuel) end
                if Enabled.FullAutoDuel then startFullAutoDuel() else stopFullAutoDuel() end
            elseif keybindKey == "OPTIMIZER" then
                Enabled.Optimizer = not Enabled.Optimizer
                if VisualSetters.Optimizer then VisualSetters.Optimizer(Enabled.Optimizer) end
                if Enabled.Optimizer then enableOptimizer() else disableOptimizer() end
            elseif keybindKey == "SPEEDSTEAL" then
                Enabled.SpeedWhileStealing = not Enabled.SpeedWhileStealing
                if VisualSetters.SpeedWhileStealing then VisualSetters.SpeedWhileStealing(Enabled.SpeedWhileStealing) end
                if Enabled.SpeedWhileStealing then startSpeedWhileStealing() else stopSpeedWhileStealing() end
            elseif keybindKey == "AUTOSTEAL" then
                Enabled.AutoSteal = not Enabled.AutoSteal
                if VisualSetters.AutoSteal then VisualSetters.AutoSteal(Enabled.AutoSteal) end
                if Enabled.AutoSteal then startAutoSteal() else stopAutoSteal() end
            end
        end
    end
end
-- ============================================================
-- INPUT HANDLER
-- ============================================================
local guiVisible = true
UserInputService.InputBegan:Connect(function(input, gpe)
    if input.KeyCode==Enum.KeyCode.Space then spaceHeld=true end
    if waitingForKeybind then
        if input.UserInputType~=Enum.UserInputType.Keyboard then return end
        if input.KeyCode==Enum.KeyCode.Unknown then return end
        local k = input.KeyCode
        KEYBINDS[waitingForKeybind] = k
        local badgeBtn = KeyBindBtns[waitingForKeybind]
        if badgeBtn then
            badgeBtn.Text = k.Name
            TweenService:Create(badgeBtn, TweenInfo.new(0.15), {BackgroundColor3 = C.badge}):Play()
        end
        waitingForKeybind = nil
        return
    end
    if gpe then return end
    if input.KeyCode==Enum.KeyCode.U then
        guiVisible=not guiVisible
        main.Visible=guiVisible; miniBtn.Visible=not guiVisible
        return
    end
    dispatchKey(input.KeyCode)
end)
UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode==Enum.KeyCode.Space then spaceHeld=false end
end)
-- ============================================================
-- CHARACTER RESPAWN
-- ============================================================
Player.CharacterAdded:Connect(function()
    task.wait(1)
    if Enabled.SpinBot then stopSpinBot(); task.wait(0.1); startSpinBot() end
    if Enabled.Galaxy then setupGalaxyForce(); adjustGalaxyJump() end
    if Enabled.SpamBat then stopSpamBat(); task.wait(0.1); startSpamBat(); startSpamBatCircle() end
    if Enabled.Unwalk then startUnwalk() end
    if Enabled.AutoSteal then startAutoSteal() end
    task.wait(0.5)
    pcall(function() local cam=workspace.CurrentCamera; if cam and Values.FOV then cam.FieldOfView=Values.FOV end end)
end)
print("INTERSTELLAR DUELS Loaded — Press U to toggle GUI.")
-- =========================================================
-- STELLA VIOLA ELEGANTE
-- ============================================================
-- =====================================================
--pastebin
-- Crea la ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
-- Crea la TextLabel principale
local textLabel = Instance.new("TextLabel")
textLabel.Parent = screenGui
textLabel.Size = UDim2.new(0, 300, 0, 40) -- più piccola
textLabel.Position = UDim2.new(0.5, -150, 0.5, -20) -- centrata
textLabel.BackgroundTransparency = 1
textLabel.Text = "https://discord.gg/TpeuntSm"
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255) -- bianco
textLabel.Font = Enum.Font["GothamBlack"] -- placeholder se Senju UI non esiste
textLabel.TextScaled = true
textLabel.TextWrapped = true
-- Bordo nero spesso (simulato con duplicati)
textLabel.TextStrokeTransparency = 0
textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
-- Funzione per creare contorno più spesso
local offsets = {Vector2.new(-1,0), Vector2.new(1,0), Vector2.new(0,-1), Vector2.new(0,1)}
for _, offset in pairs(offsets) do
    local outline = textLabel:Clone()
    outline.TextColor3 = Color3.fromRGB(0, 0, 0)
    outline.TextStrokeTransparency = 1
    outline.Position = UDim2.new(0.5, -150 + offset.X, 0.5, -20 + offset.Y)
    outline.Parent = screenGui
    outline.ZIndex = textLabel.ZIndex - 1
end
-- ============================================================
-- DISCORD WATERMARK + EFFETTI VISIVI
-- ============================================================
local function addDiscordWatermark()
    local watermark = Instance.new("TextLabel")
    watermark.Name = "DiscordWatermark"
    watermark.Parent = sg -- sg è la ScreenGui principale
    watermark.Size = UDim2.new(0, 220, 0, 24)
    watermark.Position = UDim2.new(0.5, -110, 1, -30)
    watermark.BackgroundTransparency = 1
    watermark.TextColor3 = Color3.fromRGB(200, 150, 255)
    watermark.TextStrokeTransparency = 0
    watermark.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    watermark.Font = Enum.Font.GothamBold
    watermark.TextSize = 12
    watermark.TextXAlignment = Enum.TextXAlignment.Center
    watermark.ZIndex = 50
end
task.spawn(addDiscordWatermark)
-- Arrotondamento
-- ============================================================
-- WATERMARK FINALE
-- ============================================================
task.spawn(function()
    local watermark = Instance.new("TextLabel")
    watermark.Parent = sg
    watermark.Size = UDim2.new(0, 250, 0, 20)
    watermark.Position = UDim2.new(0.5, -125, 1, -25)
    watermark.BackgroundTransparency = 1
    watermark.TextColor3 = Color3.fromRGB(200, 150, 255)
    watermark.TextStrokeTransparency = 0
    watermark.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    watermark.Font = Enum.Font.GothamBold
    watermark.TextSize = 11
    watermark.TextXAlignment = Enum.TextXAlignment.Center
    watermark.ZIndex = 50
end)
local SPEED_IDA3, SPEED_VOLTA3 = 61.2, 30
local SPEED_IDA4, SPEED_VOLTA4 = 61.2, 30
-- =============================================
--   VORTEX MINI ACTION BUTTONS - TUTTI I BOTTONI
--   Desync con Instant Respawn Originale
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Debris = game:GetService("Debris")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local isMobile = UserInputService.TouchEnabled

-- ============================================================
-- KEYBINDS
-- ============================================================
local MINI_KEYBINDS = {
    BATLOCK   = Enum.KeyCode.Unknown,
    AUTOLEFT  = Enum.KeyCode.Unknown,
    AUTORIGHT = Enum.KeyCode.Unknown,
    FLOAT     = Enum.KeyCode.Unknown,
    DROP      = Enum.KeyCode.Unknown,
    DESYNC    = Enum.KeyCode.Unknown,
}

local miniWaitingKeybind = nil
local miniKbBtns = {}

-- ============================================================
-- CREATE MINI BUTTON
-- ============================================================
local function createMiniActionBtn(posX, posY, labelTxt, accentColor, keybindKey, callback)
    local btnGui = Instance.new("ScreenGui")
    btnGui.Name = "MiniBtn_" .. keybindKey
    btnGui.ResetOnSpawn = false
    btnGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    btnGui.Parent = playerGui

    local BTN_W = isMobile and 108 or 118
    local BTN_H = isMobile and 46 or 52

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, BTN_W, 0, BTN_H)
    frame.Position = UDim2.new(0, posX, 0, posY)
    frame.BackgroundColor3 = Color3.fromRGB(8, 8, 12)
    frame.BackgroundTransparency = 0.08
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Parent = btnGui

    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)

    local stroke = Instance.new("UIStroke", frame)
    stroke.Thickness = 1.2
    stroke.Color = accentColor
    stroke.Transparency = 0.25
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local dot = Instance.new("Frame", frame)
    dot.Size = UDim2.new(0, 5, 0, 5)
    dot.Position = UDim2.new(0, 7, 0.5, -2.5)
    dot.BackgroundColor3 = accentColor
    dot.BorderSizePixel = 0
    Instance.new("UICorner", dot).CornerRadius = UDim.new(0, 3)

    local lbl = Instance.new("TextLabel", frame)
    lbl.Size = UDim2.new(1, -(16 + 34), 1, 0)
    lbl.Position = UDim2.new(0, 16, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelTxt
    lbl.TextColor3 = Color3.fromRGB(210, 210, 225)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextYAlignment = Enum.TextYAlignment.Center

    local KB_W, KB_H = 30, 18
    local kbBtn = Instance.new("TextButton", frame)
    kbBtn.Size = UDim2.new(0, KB_W, 0, KB_H)
    kbBtn.Position = UDim2.new(1, -(KB_W + 4), 0.5, -KB_H/2)
    kbBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 32)
    kbBtn.Text = (MINI_KEYBINDS[keybindKey] == Enum.KeyCode.Unknown) and "—" or MINI_KEYBINDS[keybindKey].Name
    kbBtn.TextColor3 = Color3.fromRGB(170, 170, 195)
    kbBtn.Font = Enum.Font.GothamBold
    kbBtn.TextSize = 7
    kbBtn.BorderSizePixel = 0
    kbBtn.ZIndex = frame.ZIndex + 2

    Instance.new("UICorner", kbBtn).CornerRadius = UDim.new(0, 3)

    local kbStroke = Instance.new("UIStroke", kbBtn)
    kbStroke.Thickness = 1
    kbStroke.Color = Color3.fromRGB(70, 70, 100)
    kbStroke.Transparency = 0.3

    miniKbBtns[keybindKey] = kbBtn

    -- Dragging + Click
    local dragging = false
    local didDrag = false
    local dragStart = Vector2.zero
    local frameStart = nil
    local THRESHOLD = 5

    local dragHitbox = Instance.new("TextButton", frame)
    dragHitbox.Size = UDim2.new(1, -(KB_W + 8), 1, 0)
    dragHitbox.BackgroundTransparency = 1
    dragHitbox.Text = ""
    dragHitbox.ZIndex = frame.ZIndex + 1

    local function getInputPos(inp)
        if inp.UserInputType == Enum.UserInputType.Touch then
            return Vector2.new(inp.Position.X, inp.Position.Y)
        end
        return UserInputService:GetMouseLocation()
    end

    dragHitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            didDrag = false
            dragStart = getInputPos(inp)
            frameStart = frame.Position
        end
    end)

    UserInputService.InputChanged:Connect(function(inp)
        if not dragging then return end
        if inp.UserInputType ~= Enum.UserInputType.MouseMovement and inp.UserInputType ~= Enum.UserInputType.Touch then return end
        local cur = getInputPos(inp)
        local delta = cur - dragStart
        if delta.Magnitude > THRESHOLD then
            didDrag = true
            frame.Position = UDim2.new(frameStart.X.Scale, frameStart.X.Offset + delta.X, frameStart.Y.Scale, frameStart.Y.Offset + delta.Y)
        end
    end)

    UserInputService.InputEnded:Connect(function(inp)
        if not dragging then return end
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
            dragging = false
            if not didDrag then
                TweenService:Create(frame, TweenInfo.new(0.07), {BackgroundColor3 = accentColor, BackgroundTransparency = 0.45}):Play()
                task.delay(0.16, function()
                    TweenService:Create(frame, TweenInfo.new(0.18), {BackgroundColor3 = Color3.fromRGB(8, 8, 12), BackgroundTransparency = 0.08}):Play()
                end)
                callback()
            end
            didDrag = false
        end
    end)

    frame.MouseEnter:Connect(function()
        TweenService:Create(frame, TweenInfo.new(0.12), {BackgroundTransparency = 0.0}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.12), {Transparency = 0.0}):Play()
    end)

    frame.MouseLeave:Connect(function()
        TweenService:Create(frame, TweenInfo.new(0.12), {BackgroundTransparency = 0.08}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.12), {Transparency = 0.25}):Play()
    end)

    kbBtn.MouseButton1Click:Connect(function()
        if miniWaitingKeybind == keybindKey then
            miniWaitingKeybind = nil
            kbBtn.Text = (MINI_KEYBINDS[keybindKey] == Enum.KeyCode.Unknown) and "—" or MINI_KEYBINDS[keybindKey].Name
            TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(20, 20, 32)}):Play()
            return
        end
        if miniWaitingKeybind then
            local prev = miniKbBtns[miniWaitingKeybind]
            if prev then
                local pkc = MINI_KEYBINDS[miniWaitingKeybind]
                prev.Text = (pkc == Enum.KeyCode.Unknown) and "—" or pkc.Name
                TweenService:Create(prev, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(20, 20, 32)}):Play()
            end
        end
        miniWaitingKeybind = keybindKey
        kbBtn.Text = "?"
        TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = accentColor}):Play()
    end)

    kbBtn.MouseButton2Click:Connect(function()
        MINI_KEYBINDS[keybindKey] = Enum.KeyCode.Unknown
        kbBtn.Text = "—"
        miniWaitingKeybind = nil
        TweenService:Create(kbBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(20, 20, 32)}):Play()
    end)
end

-- ============================================================
-- INPUT HANDLER KEYBINDS
-- ============================================================
UserInputService.InputBegan:Connect(function(input, gpe)
    if miniWaitingKeybind then
        if input.UserInputType ~= Enum.UserInputType.Keyboard or input.KeyCode == Enum.KeyCode.Unknown then return end
        MINI_KEYBINDS[miniWaitingKeybind] = input.KeyCode
        local badge = miniKbBtns[miniWaitingKeybind]
        if badge then
            badge.Text = input.KeyCode.Name
            TweenService:Create(badge, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(20, 20, 32)}):Play()
        end
        miniWaitingKeybind = nil
        return
    end

    if gpe then return end

    for key, kc in pairs(MINI_KEYBINDS) do
        if kc ~= Enum.KeyCode.Unknown and input.KeyCode == kc then
            if key == "BATLOCK"   then batLockCallback() end
            if key == "AUTOLEFT"  then autoLeftCallback() end
            if key == "AUTORIGHT" then autoRightCallback() end
            if key == "FLOAT"     then floatCallback() end
            if key == "DROP"      then dropCallback() end
            if key == "DESYNC"    then desyncCallback() end
        end
    end
end)

-- ============================================================
-- 1. BATLOCK (originale)
-- ============================================================
do
    local isStealingNow = false
    local lockConn3, clickConn3, chaseVel3 = nil, nil, nil
    local STEAL_SPEED3 = 50

    local function findThief3()
        local mc = player.Character if not mc then return nil end
        local mr = mc:FindFirstChild("HumanoidRootPart") if not mr then return nil end
        local best, bd = nil, math.huge
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local r = p.Character:FindFirstChild("HumanoidRootPart")
                if r then local d=(r.Position-mr.Position).Magnitude if d<bd then bd=d;best=p end end
            end
        end
        return best
    end

    local function stopLockOn3()
        if lockConn3 then lockConn3:Disconnect(); lockConn3=nil end
        if chaseVel3 and chaseVel3.Parent then chaseVel3:Destroy(); chaseVel3=nil end
    end

    local function startLockOn3(target)
        stopLockOn3()
        lockConn3 = RunService.Heartbeat:Connect(function()
            if not isStealingNow then return end
            local mc,tc = player.Character, target and target.Character
            if not mc or not tc then return end
            local mr = mc:FindFirstChild("HumanoidRootPart")
            local tr = tc:FindFirstChild("HumanoidRootPart")
            if not mr or not tr then return end
            local dist = (tr.Position-mr.Position).Magnitude
            local dir = (tr.Position-mr.Position).Unit
            if dist > 3 then
                if not chaseVel3 or not chaseVel3.Parent then
                    chaseVel3 = Instance.new("BodyVelocity")
                    chaseVel3.MaxForce = Vector3.new(1e5,1e5,1e5)
                    chaseVel3.P = 1e5; chaseVel3.Parent = mr
                end
                chaseVel3.Velocity = Vector3.new(dir.X*STEAL_SPEED3,dir.Y*STEAL_SPEED3,dir.Z*STEAL_SPEED3)
            else
                if chaseVel3 and chaseVel3.Parent then chaseVel3:Destroy();chaseVel3=nil end
            end
        end)
    end

    local function equipBat3()
        local c = player.Character if not c then return end
        local hum = c:FindFirstChildOfClass("Humanoid") if not hum then return end
        hum:UnequipTools(); task.wait(0.1)
        local bp = player:FindFirstChildOfClass("Backpack")
        if bp then
            for _,tool in ipairs(bp:GetChildren()) do
                if tool:IsA("Tool") and tool.Name:lower():find("bat") then
                    hum:EquipTool(tool); return
                end
            end
        end
    end

    local function setStealing3(state)
        if state == isStealingNow then return end
        isStealingNow = state
        if state then
            local target = findThief3(); equipBat3()
            if target then startLockOn3(target) end
            if clickConn3 then clickConn3:Disconnect() end
            clickConn3 = RunService.Heartbeat:Connect(function()
                if not isStealingNow then return end
                local c = player.Character
                local tool = c and c:FindFirstChildOfClass("Tool")
                if tool then pcall(function() tool:Activate() end) end
            end)
        else
            stopLockOn3()
            if clickConn3 then clickConn3:Disconnect(); clickConn3=nil end
        end
    end

    local batLockToggle = false
    function batLockCallback()
        batLockToggle = not batLockToggle
        setStealing3(batLockToggle)
    end

    createMiniActionBtn(10, 80, "BatLock", Color3.fromRGB(0,200,255), "BATLOCK", batLockCallback)
end

-- ============================================================
-- 2. AUTO LEFT (originale)
-- ============================================================
do
    local A1_P1 = Vector3.new(-472.59,-7.30,94.43)
    local A1_P2 = Vector3.new(-484.55,-5.33,95.05)
    local A1_P3 = Vector3.new(-472.59,-7.30,94.43)
    local A1_P4 = Vector3.new(-471.25,-6.83,7.08)
    local autoL = false
    local igCache3, instaG3 = {}, false

    RunService.Heartbeat:Connect(function()
        if not instaG3 then return end
        local r = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if not r then return end
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") then
                local p = v.Parent
                local pos2 = p:IsA("Attachment") and p.WorldPosition or (p:IsA("BasePart") and p.Position)
                if pos2 and (pos2-r.Position).Magnitude < 7 then
                    if not igCache3[v] then
                        local data = {hold={},trigger={},ready=true}
                        pcall(function()
                            for _,c in pairs(getconnections(v.PromptButtonHoldBegan)) do if c.Function then table.insert(data.hold,c.Function) end end
                            for _,c in pairs(getconnections(v.Triggered)) do if c.Function then table.insert(data.trigger,c.Function) end end
                        end)
                        igCache3[v] = data
                    end
                    local d = igCache3[v]
                    if d and d.ready then
                        d.ready = false
                        task.spawn(function()
                            for _,fn in pairs(d.hold) do task.spawn(fn) end
                            task.wait(0.15)
                            for _,fn in pairs(d.trigger) do task.spawn(fn) end
                            task.wait(0.05); d.ready = true
                        end)
                    end
                end
            end
        end
    end)

    local function go3(pos, spd, cond)
        local r = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if not r then return end
        while cond() and (r.Position-pos).Magnitude > 1 do
            local d = (pos-r.Position).Unit
            r.AssemblyLinearVelocity = Vector3.new(d.X*spd, r.AssemblyLinearVelocity.Y, d.Z*spd)
            task.wait()
        end
    end

    function autoLeftCallback()
        autoL = not autoL
        if autoL then
            task.spawn(function()
                go3(A1_P1, SPEED_IDA3, function() return autoL end)
                go3(A1_P2, SPEED_IDA3, function() return autoL end)
                instaG3 = true
                go3(A1_P3, SPEED_VOLTA3, function() return autoL end)
                instaG3 = false
                go3(A1_P4, SPEED_VOLTA3, function() return autoL end)
                autoL = false
            end)
        else
            instaG3 = false
            local r = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if r then r.AssemblyLinearVelocity = Vector3.new(0, r.AssemblyLinearVelocity.Y, 0) end
        end
    end

    createMiniActionBtn(10, 122, "Auto Left", Color3.fromRGB(100,200,255), "AUTOLEFT", autoLeftCallback)
end

-- ============================================================
-- 3. AUTO RIGHT (originale)
-- ============================================================
do
    local B1 = Vector3.new(-474.02,-7.30,25.55)
    local B2 = Vector3.new(-484.92,-5.13,24.53)
    local B3 = Vector3.new(-474.02,-7.30,25.55)
    local B4 = Vector3.new(-470.93,-6.83,113.38)
    local autoR = false

    local function go4(pos, spd, cond)
        local r = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if not r then return end
        while cond() and (r.Position-pos).Magnitude > 1 do
            local d = (pos-r.Position).Unit
            r.AssemblyLinearVelocity = Vector3.new(d.X*spd, r.AssemblyLinearVelocity.Y, d.Z*spd)
            task.wait()
        end
    end

    function autoRightCallback()
        autoR = not autoR
        if autoR then
            task.spawn(function()
                go4(B1, SPEED_IDA4, function() return autoR end)
                go4(B2, SPEED_IDA4, function() return autoR end)
                go4(B3, SPEED_VOLTA4, function() return autoR end)
                go4(B4, SPEED_VOLTA4, function() return autoR end)
                autoR = false
            end)
        else
            local r = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if r then r.AssemblyLinearVelocity = Vector3.new(0, r.AssemblyLinearVelocity.Y, 0) end
        end
    end

    createMiniActionBtn(10, 164, "Auto Right", Color3.fromRGB(100,200,255), "AUTORIGHT", autoRightCallback)
end

-- ============================================================
-- 4. FLOAT (originale)
-- ============================================================
do
    local floatRunning = false
    local floatSpd4 = 55

    local function slowDown4()
        local c = player.Character
        local root = c and c:FindFirstChild("HumanoidRootPart") if not root then return end
        local tratti = {{vel = -85, pausa = 0.02},{vel = -96, pausa = 0.03},{vel = -87, pausa = 0.02}}
        for _, t in ipairs(tratti) do
            if not floatRunning then break end
            local bv = Instance.new("BodyVelocity")
            bv.Velocity = Vector3.new(0, t.vel, 0)
            bv.MaxForce = Vector3.new(0, math.huge, 0)
            bv.P = 9999
            bv.Parent = root
            Debris:AddItem(bv, t.pausa)
            task.wait(t.pausa)
        end
    end

    local function autoFloat4()
        while floatRunning do
            local c = player.Character
            local hum = c and c:FindFirstChildOfClass("Humanoid")
            local hrp = c and c:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                local bv = Instance.new("BodyVelocity")
                bv.Velocity = Vector3.new(0, floatSpd4, 0)
                bv.MaxForce = Vector3.new(0, math.huge, 0)
                bv.P = 3000
                bv.Parent = hrp
                Debris:AddItem(bv, 0.3)
                task.delay(0.52, slowDown4)
                repeat task.wait() until hum.FloorMaterial ~= Enum.Material.Air
            end
            task.wait(0.1)
        end
    end

    function floatCallback()
        floatRunning = not floatRunning
        if floatRunning then task.spawn(autoFloat4) end
    end

    createMiniActionBtn(10, 206, "Float", Color3.fromRGB(255,140,60), "FLOAT", floatCallback)
end

-- ============================================================
-- 5. DROP (originale)
-- ============================================================
do
    local dropConns = {}
    local dropActive = false

    function dropCallback()
        if dropActive then return end
        dropActive = true
        for _,c in ipairs(dropConns) do if typeof(c) == "RBXScriptConnection" then c:Disconnect() end end
        dropConns = {}

        local char = player.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then dropActive = false; return end

        local colConn = RunService.Stepped:Connect(function()
            for _,p in ipairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    for _,part in ipairs(p.Character:GetChildren()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end
        end)
        table.insert(dropConns, colConn)

        task.spawn(function()
            while dropActive do
                RunService.Heartbeat:Wait()
                local c2 = player.Character
                local root2 = c2 and c2:FindFirstChild("HumanoidRootPart")
                if not root2 then break end
                local vel = root2.Velocity
                root2.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
                RunService.RenderStepped:Wait()
                if root2 and root2.Parent then root2.Velocity = vel end
                RunService.Stepped:Wait()
                if root2 and root2.Parent then root2.Velocity = vel + Vector3.new(0, 0.1, 0) end
            end
        end)

        task.delay(0.3, function()
            dropActive = false
            for _,c in ipairs(dropConns) do if typeof(c) == "RBXScriptConnection" then c:Disconnect() end end
            dropConns = {}
        end)
    end

    createMiniActionBtn(10, 248, "Drop", Color3.fromRGB(255,75,75), "DROP", dropCallback)
end

-- ============================================================
-- 6. DESYNC (con Insta Reset ORIGINALE)
-- ============================================================
do
    local desyncActive = false
    local hookConnection = nil
    local hasResetBeenDone = false
    local resetTargetPart = nil

    task.spawn(function()
        local craftPos = workspace:WaitForChild("CraftingMachine", 5)
        if craftPos and craftPos:FindFirstChild("VFX") and craftPos.VFX:FindFirstChild("Secret") then
            resetTargetPart = craftPos.VFX.Secret:WaitForChild("SoundPart", 2)
        end
    end)

    local function executeInstaReset()
        if hasResetBeenDone then return end
        if not resetTargetPart then return end
        
        hasResetBeenDone = true
        
        local character = player.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        local root = character and character:FindFirstChild("HumanoidRootPart")
        local camera = workspace.CurrentCamera
        
        if humanoid and root then
            local camCF = camera.CFrame
            camera.CameraType = Enum.CameraType.Scriptable
            camera.CFrame = camCF
            
            local head = character:FindFirstChild("Head")
            if root:FindFirstChild("Died") then root.Died:Destroy() end
            if head and head:FindFirstChild("Died") then head.Died:Destroy() end
            
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    part.Transparency = 1
                elseif part:IsA("ParticleEmitter") or part:IsA("Trail") or part:IsA("Beam") then
                    part.Enabled = false
                end
            end
            
            root.CFrame = resetTargetPart.CFrame
            humanoid:ChangeState(Enum.HumanoidStateType.Dead)
            
            task.spawn(function()
                pcall(function() player:LoadCharacter() end)
            end)
        end
        
        task.spawn(function()
            local newChar = player.CharacterAdded:Wait()
            local newHum = newChar:WaitForChild("Humanoid", 3)
            if newHum then
                camera.CameraType = Enum.CameraType.Custom
                camera.CameraSubject = newHum
            end
            hasResetBeenDone = false
        end)
    end

    local function vortexDesyncHook(packet)
        if packet.PacketId == 0x1B then
            local data = packet.AsBuffer
            buffer.writeu32(data, 1, 0xFFFFFFFF)
            buffer.writeu32(data, 5, 0xFFFFFFFF)
            buffer.writeu32(data, 9, 0xFFFFFFFF)
            packet:SetData(data)
        end
    end

    function desyncCallback()
        desyncActive = not desyncActive
        if desyncActive then
            if raknet then
                hookConnection = raknet.add_send_hook(vortexDesyncHook)
            end
            executeInstaReset()   -- Instant Respawn
        else
            if hookConnection then
                hookConnection:Disconnect()
                hookConnection = nil
            end
        end
    end

    createMiniActionBtn(10, 290, "Desync", Color3.fromRGB(80, 255, 100), "DESYNC", desyncCallback)
end

-- Noclip originale
do
    local function addNC(p1, p2)
        if not p1 or not p2 or p1 == p2 then return end
        local nc = Instance.new("NoCollisionConstraint")
        nc.Part0 = p1; nc.Part1 = p2; nc.Parent = p1
    end

    local function linkAll()
        local myChar = player.Character if not myChar then return end
        for _,op in ipairs(Players:GetPlayers()) do
            if op ~= player and op.Character then
                for _,oPart in ipairs(op.Character:GetDescendants()) do
                    if oPart:IsA("BasePart") then
                        for _,mPart in ipairs(myChar:GetDescendants()) do
                            if mPart:IsA("BasePart") then addNC(mPart, oPart) end
                        end
                    end
                end
            end
        end
    end

    linkAll()
    Players.PlayerAdded:Connect(function(p)
        p.CharacterAdded:Connect(function() task.wait(0.5); linkAll() end)
    end)
    player.CharacterAdded:Connect(function() task.wait(0.5); linkAll() end)
end

print("VORTEX MINI BUTTONS COMPLETI + DESYNC (Instant Respawn) LOADED")
print("========================================")
print("VORTEX DUELS - COMPLETELY LOADED!")
print("Press U to toggle GUI")
print("========================================")
print("========================================")
print("VORTEX DUELS - COMPLETELY LOADED!")
print("Press U to toggle GUI")
print("========================================")